# Handoff — control plane phase 1 (2026-08-22)

Everything that lived only on the previous machine, so the work continues elsewhere.

- `.claude/GOAL-archive-*.md` — every archived mission, including `GOAL-archive-mcp-control-plane.md` (this one, with its full progress log).
- `.claude/roadmap/` — the work packages (WP-01..14, DISPATCH.md, README.md) and `CP-01-control-plane-fase-2.md`, the package for what remains of the MVP (snapshots, scene, 5b annotate, 5c evidence).
- `.claude/refs/` — reference notes.
- `.claude/handoff/memory/` — the agent's project memory files from the old machine (copy them to `~/.claude/projects/<project>/memory/` on the new one; `MEMORY.md` is the index).
- `.claude/handoff/scratchpad/` — the final mission report, the 5b/5c/scene handoff notes, and the PR bodies as posted.

Open PRs (merge in order; each re-targets when its base lands): #220, #213 → #221 → #222 → #223.
