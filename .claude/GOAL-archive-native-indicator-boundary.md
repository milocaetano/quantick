# Native indicator boundary mission

Dock existing native indicators through IndicatorSlots and enforce exact root/port shapes plus separate bounded root implementation budgets, so focused ownership can grow without renewed shared-root concentration.

**Tier:** high, because actual operation ownership and repository enforcement require independent source-first proof.

## Request ledger

- **R1** Allocate native attachment through the existing IndicatorHost. -> A1.
- **R2** Record the full native TabSlot and SavedKind through IndicatorSlots. -> A2.
- **R3** Resolve focus once in the root adapter and supply the existing context. -> A3.
- **R4** Mirror returned layout intent and note exactly one save. -> A4.
- **R5** Retain two host effects and five context collections without unrelated variant changes. -> A5.
- **R6** Preserve exact native ID and empty values/default handling. -> A6.
- **R7** Preserve human ownership and complete target bookkeeping. -> A7.
- **R8** Preserve unknown-ID visible error slots with no EMA fallback. -> A8.
- **R9** Preserve toolbar/control reachability, script validation and permissions. -> A9.
- **R10** Preserve saved-native restore and settings/library/hot-reload behavior. -> A10.
- **R11** Compile an independent fake host driving the actual known and unknown native operation with independently specified source, saved-kind, ownership and removal expectations. -> A11.
- **R12** Prove actual native toolbar attach/remove, two-pane mirroring and save intent. -> A12.
- **R13** Retain and pass native catalog, unknown/saved-unknown native, F1 script and foreign-workspace registration/refusal tests. -> A13.
- **R14** Enforce normalized field names, types and visibility for QuantickApp, ChartState and IndicatorSlots. -> A14.
- **R15** Enforce exact IndicatorHost signatures. -> A15.
- **R16** Enforce through GUARDS, check, check_file, CLI and workspace tests with target/path diagnostics. -> A16.
- **R17** Reject equal-count broad-root type replacement, root field addition and new host effects without source-hash or cardinality substitutes. -> A17.
- **R18** Independently measure origin-SHA union production source-line spans of protected declarations and explicit inherent/trait implementations across all app source files. -> A18.
- **R19** Apply separate no-growth caps so cross-root transfer cannot finance growth. -> A19.
- **R20** Preserve totals under moves/splits of the same broad implementation. -> A20.
- **R21** Allow focused owner implementation growth in existing and new files outside root totals. -> A21.
- **R22** Apply no arbitrary registration exemption; add no root registration for this existing variant. -> A22.
- **R23** Reuse sound production classification, ratchet arithmetic/registry and scratch infrastructure without changing meanings or ceilings. -> A23.
- **R24** Document and test lexical comments, strings, raw strings, lifetimes, balanced delimiters and qualified/multiline implementation grammar. -> A24.
- **R25** Document and test target fields, visibility and current lifetimes. -> A25.
- **R26** Diagnose missing/duplicate targets, unreadable/corrupt source/baselines and unsupported protected syntax. -> A26.
- **R27** Diagnose explicit root-renaming import/type aliases and detectable protected macro/external-path/include scope while retaining callback aliases and unrelated declaration macros. -> A27.
- **R28** Document synthesized-name, indirect-helper, root-carrying free-function and net-neutral rewrite limits without claiming macro expansion or general type resolution; tests must not advertise those known gaps as rejected cases. -> A28.
- **R29** Use negative scratch and CLI fixtures for field/effect/type broadening, below-file-ceiling root deposits, sibling splits, trait growth, cross-root transfers, qualified/multiline and malformed/missing/unsupported inputs. -> A29.
- **R30** Use realistic native-owned extension positive fixtures and equivalent broad-root negative fixtures; reject a whole-directory budget design. -> A30.
- **R31** Record origin/final SHA, exact shape/budget contributions and semantic before/after responsibility changes. -> A31.
- **R32** Record realistic variant evidence and independent commands/results. -> A32.
- **R33** Classify rates as rare attachment or repository tooling; measure any touched hot path. -> A33.
- **R34** Run ordered fmt/clippy/build/workspace tests before every commit and guards after edits. -> A34.
- **R35** Close independent architecture, AI and source-first delivery reviews plus exact-head CI before merge. -> C2 (mandatory pending closing obligation; historical A35).
- **R36** Preserve runtime/public/financial behavior, score rubric and advisory blast report; do not promote its permissive parser to enforcement; add no indicator/capability/root field/registry arm/dependency/general resolver. -> A36.
- **R37** Reassess the integrated SHA with unchanged rubric and native/foreign-workspace evidence without automatically claiming SE2=5 or complete semantic coupling analysis. -> C4 (mandatory pending post-integration closing obligation; historical A37).
- **R38** Record blocker and revise campaign plan if the declared bounded scanner requires a general resolver or broad exemptions; never silently weaken criteria. -> A38.
- **R39** Start from the latest integrated campaign SHA and recheck measured origin against intervening work without silently raising caps. -> A39; C3 for any later base advance.
- **R40** Exclude Q2 schema builders, Q3 lane transport, Q4 Windows authority and Q5 workspace installation ownership. -> A40.
- **R41** Retain this task's unique mission and evidence artifacts. -> A41; C1.
- **R42** Keep owner/branch/worktree/PR/head/Project item null until their claim/readback, then record those six actual identity fields. -> A42.
- **R43** Respect D4's maximum three independent disjoint implementation worktrees, serialized campaign merges and revalidation after base advance. -> A43 for current execution records; C3 for the pending merge and any later base advance.
- **R44** Reserve main merging exclusively for the human. -> A44 for the current authority boundary; C3 for continuing enforcement.
- **R45** Retain evidence in this issue, committed mission/contract/dossier, raw guard/behavior checks and linked PR/CI. -> A45 for actual available records; C2/C3 for final-head gate and integration publication.
- **R46** Start operation/repair counters at zero and preserve cumulative per-signature history. -> A46.
- **R47** Use no paid services or deployment, and preserve financial rules and the score rubric. -> A47; A36.

## Decisions

D1: Coordinator assigned issue #338 on feat/native-indicator-boundary, campaign base origin/campaign/architecture-a at 0bd50f9b815a05e2ba8d0c9804324dbb415f6658, and retains remote/review/merge actions.

## Assumptions

S1: No clarification qualifies: issue explicitly bounds scanner scope, aliases, known limitations and authority.
S2: Use normalized source tokens, not semantic type equality, as expressly requested. Source grammar choices are reversible and documented.
S3: Existing UI behavior is unchanged; rare native attachment and repository tooling are the only changed rates.

## Acceptance criteria

- [x] **A1** Allocate native attachment through the existing IndicatorHost. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R1)*
- [x] **A2** Record the full native TabSlot and SavedKind through IndicatorSlots. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R2)*
- [x] **A3** Resolve focus once in the root adapter and supply the existing context. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R3)*
- [x] **A4** Mirror returned layout intent and note exactly one save. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R4)*
- [x] **A5** Retain two host effects and five context collections without unrelated variant changes. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R5)*
- [x] **A6** Preserve exact native ID and empty values/default handling. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R6)*
- [x] **A7** Preserve human ownership and complete target bookkeeping. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R7)*
- [x] **A8** Preserve unknown-ID visible error slots with no EMA fallback. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R8)*
- [x] **A9** Preserve toolbar/control reachability, script validation and permissions. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R9)*
- [x] **A10** Preserve saved-native restore and settings/library/hot-reload behavior. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R10)*
- [x] **A11** Compile an independent fake host driving the actual known and unknown native operation with independently specified source, saved-kind, ownership and removal expectations. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R11)*
- [x] **A12** Prove actual native toolbar attach/remove, two-pane mirroring and save intent. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R12)*
- [ ] **A13** Retain and pass native catalog, unknown/saved-unknown native, F1 script and foreign-workspace registration/refusal tests. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R13)*
- [x] **A14** Enforce normalized field names, types and visibility for QuantickApp, ChartState and IndicatorSlots. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R14)*
- [x] **A15** Enforce exact IndicatorHost signatures. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R15)*
- [x] **A16** Enforce through GUARDS, check, check_file, CLI and workspace tests with target/path diagnostics. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R16)*
- [x] **A17** Reject equal-count broad-root type replacement, root field addition and new host effects without source-hash or cardinality substitutes. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R17)*
- [x] **A18** Independently measure origin-SHA union production source-line spans of protected declarations and explicit inherent/trait implementations across all app source files. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R18)*
- [x] **A19** Apply separate no-growth caps so cross-root transfer cannot finance growth. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R19)*
- [x] **A20** Preserve totals under moves/splits of the same broad implementation. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R20)*
- [x] **A21** Allow focused owner implementation growth in existing and new files outside root totals. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R21)*
- [x] **A22** Apply no arbitrary registration exemption; add no root registration for this existing variant. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R22)*
- [x] **A23** Reuse sound production classification, ratchet arithmetic/registry and scratch infrastructure without changing meanings or ceilings. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R23)*
- [x] **A24** Document and test lexical comments, strings, raw strings, lifetimes, balanced delimiters and qualified/multiline implementation grammar. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R24)*
- [x] **A25** Document and test target fields, visibility and current lifetimes. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R25)*
- [x] **A26** Diagnose missing/duplicate targets, unreadable/corrupt source/baselines and unsupported protected syntax. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R26)*
- [x] **A27** Diagnose explicit root-renaming import/type aliases and detectable protected macro/external-path/include scope while retaining callback aliases and unrelated declaration macros. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R27)*
- [x] **A28** Document synthesized-name, indirect-helper, root-carrying free-function and net-neutral rewrite limits without claiming macro expansion or general type resolution; tests must not advertise those known gaps as rejected cases. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R28)*
- [x] **A29** Use negative scratch and CLI fixtures for field/effect/type broadening, below-file-ceiling root deposits, sibling splits, trait growth, cross-root transfers, qualified/multiline and malformed/missing/unsupported inputs. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R29)*
- [x] **A30** Use realistic native-owned extension positive fixtures and equivalent broad-root negative fixtures; reject a whole-directory budget design. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R30)*
- [ ] **A31** Record origin/final SHA, exact shape/budget contributions and semantic before/after responsibility changes. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R31)*
- [ ] **A32** Record realistic variant evidence and independent commands/results. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R32)*
- [x] **A33** Classify rates as rare attachment or repository tooling; measure any touched hot path. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R33)*
- [ ] **A34** Run ordered fmt/clippy/build/workspace tests before every commit and guards after edits. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R34)*
- [x] **A36** Preserve runtime/public/financial behavior, score rubric and advisory blast report; do not promote its permissive parser to enforcement; add no indicator/capability/root field/registry arm/dependency/general resolver. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R36)*
- [x] **A38** Record blocker and revise campaign plan if the declared bounded scanner requires a general resolver or broad exemptions; never silently weaken criteria. *Evidence:* source diff, named fixture/command outputs and independent assessment -> `docs/quality/native-indicator-boundary-evidence.md`, raw `Q6-validation/` artifacts. *(R38)*
- [ ] **A39** The recorded starting base and measured origin agree with the latest integrated campaign SHA; comparison to the current base identifies any intervening work without silently raising caps. *Evidence:* actual Git ancestry/base identity, origin measurement and frozen command receipts -> `Q6-validation/mission-write-receipt.json`, `Q6-validation/origin-sha.txt`, `Q6-validation/origin-crosscheck.json`, `Q6-validation/delivery-ledger-repair/frozen-proof.json`, and current review dossier `identity.json`. Any subsequent base advance requires C3 revalidation. *(R39)*
- [ ] **A40** The branch diff and task ownership records retain the explicit Q2/Q3/Q4/Q5 exclusions. *Evidence:* full campaign-base diff and claimed file responsibilities -> current review dossier `branch.diff` and `github-objective-proof/coordinator-host-and-commit-journal.json`. *(R40)*
- [ ] **A41** This task has its own committed mission, contract and evidence dossier, identified with issue #338 and child PR #346. *Evidence:* branch-added archive, both exact contract/budget baselines and issue-linked dossier -> `.claude/GOAL-archive-native-indicator-boundary.md`, `crates/guards/extension-shapes-baseline.txt`, `crates/guards/extension-roots-baseline.txt`, `docs/quality/native-indicator-boundary-evidence.md`, current review dossier `branch.diff` and `identity.json`. *(R41)*
- [ ] **A42** Actual claim/readback records identify Q6's owner, branch, worktree, PR, head and Project item; identity fields are populated from their corresponding completed operations. *Evidence:* public claim and operation readbacks, actual Git/PR identity -> [initial Q6 claim checkpoint](https://github.com/milocaetano/quantick/issues/330#issuecomment-5578317494), [PR publication checkpoint](https://github.com/milocaetano/quantick/issues/330#issuecomment-5579013310), current review dossier `identity.json` and `github-objective-proof/coordinator-host-and-commit-journal.json`. *(R42)*
- [ ] **A43** Recorded Q6 implementation ownership and host windows respect D4's three-worktree maximum and disjoint responsibility boundaries. *Evidence:* the actual claim, serialized host grants and commit/operation journals -> current review dossier `github-objective-proof/coordinator-host-and-commit-journal.json` and [this repair's exclusive-host checkpoint](https://github.com/milocaetano/quantick/issues/330#issuecomment-5579747376). The pending campaign merge and future base-advance revalidation remain C3, not completed outcomes. *(R43)*
- [ ] **A44** Q6's actual branch/PR and recorded operations target the campaign branch while retaining the explicit human-only main-merge boundary. *Evidence:* actual Git/PR identity and completed operation records -> current review dossier `identity.json` and `github-objective-proof/coordinator-host-and-commit-journal.json`; C3 retains the future authority boundary. *(R44)*
- [ ] **A45** The task's reviewable evidence inventory links issue #338, its committed mission/contract/dossier, raw guard/behavior checks and actual PR/CI records with their tested identities. *Evidence:* [issue #338](https://github.com/milocaetano/quantick/issues/338), [PR #346](https://github.com/milocaetano/quantick/pull/346), the A41 committed paths, `Q6-validation/016-registered-guards.log`, `017-indicator-operations.log`, `018-native-catalog-and-errors.log`, `019-foreign-workspace.log`, retained `Q6-CI-34188161228-observed.json`, current review dossier `identity.json`, and final commit/CI records published by C2/C3. Historical records retain their original head; they do not prove a later head's pending gate. *(R45)*
- [ ] **A46** Actual campaign operations retain their zero-start initialization and cumulative per-signature operation/repair history, including original failures and bounded attempts. *Evidence:* initial claim and subsequent public journal records, raw original failed executions and local cumulative ledger -> [initial Q6 claim checkpoint](https://github.com/milocaetano/quantick/issues/330#issuecomment-5578317494), current review dossier `github-objective-proof/coordinator-host-and-commit-journal.json`, `Q6-validation/counters.json`, `007-origin-inventory.log`, `009-origin-inventory.log`, `025-final-workspace-tests.log`, and `ai-nested-impl-repair/040-frozen-workspace-tests.log`. *(R46)*
- [ ] **A47** The Q6 change and recorded task operations stay within local implementation, validation and authorized GitHub delivery, with no paid-service provisioning, deployment, financial-rule change or score-rubric edit. *Evidence:* full branch diff, exact raw command/commit receipts and published operation records -> current review dossier `branch.diff`, `github-objective-proof/coordinator-host-and-commit-journal.json`, `Q6-validation/first-commit-proof.json`, `ai-nested-impl-repair/frozen-proof.json`, and `delivery-ledger-repair/frozen-proof.json`. *(R47)*

## Injected gates

- [ ] **G1** All artifacts satisfy the English rule. Evidence: guard output and architecture dimension 8 -> evidence dossier.
- [ ] **G2** Ordered fmt/clippy/build/workspace checks pass at the final campaign base before each commit. Evidence: raw Q6-validation logs and frozen-tree receipt -> evidence dossier.
- [x] **G3** Performance rates declared and any hot-path impact measured. Evidence: changed-path classification -> evidence dossier.
- [ ] **G4** Architecture review closes every Blocker/Should-fix. Evidence: coordinator independent review -> PR dossier.

## Non-applicable gates

No engine changes: no bar-building or financial algorithm. No changed visible surface or action: native toolbar and control behavior remain existing, covered by integration tests; visual QA, new env hooks and trader UX gates do not apply to this ownership-only change. No new product capability: no new-extension registration or fake new capability; actual existing variant fake-host fixture remains required. No hot-path changes are planned.

## Closing steps

These are mandatory lifecycle obligations, not pre-merge A/G outcomes. R35 and R37 remain explicitly pending closing ledger rows; they are neither claimed COVERED/DELIVERED nor deferred. Their historical acceptance placements were the checklist defect identified by independent review. Correcting placement does not perform either closing event.

C1: Archive this complete mission before reviews as `.claude/GOAL-archive-native-indicator-boundary.md`. The committed archive exists; each repair remains subject to fresh required checks and reviews. (R41)
C2: **Pending for the resulting candidate head.** Close independent architecture, AI and source-first delivery reviews plus exact-head CI before merge. This includes actual full delivery PASS, completed architecture/AI reviews and zero unresolved required findings; none is implied by this checklist correction. Preserve the completed reports/CI with their actual heads and publish fresh resulting-head closure before C3. (R35, R45; historical A35)
C3: **Pending campaign integration.** Coordinator finishes child PR #346 against campaign/architecture-a only after C2, serializes the authorized merge, verifies actual parent/head/tree and merge readback, and retains disjoint-worktree/host controls. If the campaign base advances, recheck the measured origin against intervening work without silently raising caps, update the affected branch and revalidate required tests/reviews/CI before merging. Main merging remains exclusively human; no direct main push, auto-merge, queue, protection bypass or agent main merge is authorized. Retain actual publication/Project/readback evidence. (R39, R43, R44, R45)
C4: **Pending after actual campaign integration.** Reassess the integrated SHA with unchanged rubric and native/foreign-workspace evidence without automatically claiming SE2=5 or complete semantic coupling analysis. Record the actual integrated commit/readback and evidence-backed quantick-score v1.0 assessment; a child-candidate assessment cannot substitute. The coordinator must complete this step after C3 and use it to select further campaign work. Main's score and final human-integration criteria remain separate and explicitly pending. (R37; historical A37)

### Historical acceptance-to-closing crosswalk

| Historical ID | Complete obligation retained | Current destination and status |
| --- | --- | --- |
| A35 | Close independent architecture, AI and source-first delivery reviews plus exact-head CI before merge. | R35 -> C2, pending for the resulting candidate head; not an A/G grade or approved deferral. |
| A37 | Reassess the integrated SHA with unchanged rubric and native/foreign-workspace evidence without automatically claiming SE2=5 or complete semantic coupling analysis. | R37 -> C4, pending actual post-integration assessment; not an A/G grade or approved deferral. |

## Evidence status before frozen commit validation

Observed source-tree proof: `016-registered-guards.log` passed all registered guards and independent scanner/scratch/CLI fixtures; `017-indicator-operations.log` passed seven actual native/F1 operation tests; `018-native-catalog-and-errors.log` passed nine native catalog/default/error/restore tests; `019-foreign-workspace.log` passed 15 bundle tests including foreign registration-only roundtrip and refusal. Exact commands and exits are retained in matching JSON receipts. Origin independent span agreement is in `origin-crosscheck.json`; exact contracts and all 24 contributions are in the evidence dossier.

A13/A31/A32 still require final-head identification/independent reproduction even though their source-tree fixture and origin portions have passed. A34/G2 require actual ordered final command receipts and coordinator frozen-tree journaling before any commit. Historical A35/A37 now map to pending C2/C4; G4 still requires actual architecture closure. These remain the coordinator's independent review/CI/integrated-SHA obligations. G1's final archive/prose must pass the final guard run. These obligations are retained rather than checked prematurely. The final commit records its own identity through the external head receipt; no self-referential hash is fabricated inside the archive.

## First AI review repair status

Independent finding AI-Q6-1 on candidate `44822b94b16d1f1cbf98c48ff71fea8b69936e46` demonstrated uncharged explicit root implementations inside parenthesized and array const blocks. The coordinator authorized repair attempt one before edits in [checkpoint 5579218136](https://github.com/milocaetano/quantick/issues/330#issuecomment-5579218136). The correction belongs to the existing R18/R24/R29 accounting and negative-fixture requirements, with no contract/cap change. The evidence dossier links the actual finding and original compiler/guard failures. New source-tree validation and review closure remain pending until their actual receipts are recorded externally. The original pre-code mission and all previous failed results remain immutable; no acceptance criterion or repair counter is reset.

The scanner correction's guards and original reviewer specimens passed, then default-harness workspace execution `040` failed the existing live-client Success assertion after ordered fmt/clippy/build passed. The evidence dossier records the unknown actual response, preserved original log and source-based contention hypothesis. The coordinator authorized one bounded retry in [checkpoint 5579347919](https://github.com/milocaetano/quantick/issues/330#issuecomment-5579347919), with child-only `RUST_TEST_THREADS=1` for the complete workspace test after fresh guards and ordered checks. This changes harness scheduling only; all tests, their internal concurrency, assertions and thresholds remain unchanged. Final results must state this environment, and unchanged default CI remains required. No source fix or cause for the existing live-client failure is claimed.

## Delivery checklist correction

Independent source-first review at `9188e4c375cd377883a5e32318b51c7d61afd68d` found eleven missing source mappings; the bounded strong criteria pass confirmed the A35/A37 lifecycle-placement defects. [The actual failed report](https://github.com/milocaetano/quantick/pull/346#issuecomment-5579746859) and [authorized repair attempt one](https://github.com/milocaetano/quantick/issues/330#issuecomment-5579747376) precede this archive-only edit. R28/R36 explicitly retain both scanner-honesty prohibitions, and R39-R47 cover the nine omitted campaign clauses. Original R numbers and complete R35/R37 obligations remain; their C2/C4 destinations and historical A35/A37 crosswalk keep future work mandatory and pending. No review result or future integration is claimed by moving a row.

Current review dossier evidence is under `C:/Users/camil/AppData/Local/Temp/quantick-architecture-a/Q6-review-9188e4c375cd/`; its thirteen selected public operation/host/commit journals are actual GitHub readbacks, not an exhaustive interaction transcript. After this repair, the coordinator supplies a fresh exact-head dossier and current operation/identity records. Raw validation paths are relative to `C:/Users/camil/AppData/Local/Temp/quantick-architecture-a/Q6-validation/`, with CI observations beside that directory. Named historical raw files retain their original source/head and are not silently promoted to later-head proof. The new `delivery-ledger-repair/` receipt records original-request byte equality, all fourteen branch-file hashes, the sole archive change and ordered checks, with child-only `RUST_TEST_THREADS=1` for the full workspace execution. The original mission SHA-256 `d86b6f9151e0d35bb2b9a2547016ee4f1ea3ee9c33ceab694a5d712fb82950bf`, original failure outputs and cumulative counters remain retained. No new source, test, workflow, rubric, baseline, financial rule or deployment change belongs to this repair.

## Request as received

Attributed verbatim source: GitHub issue https://github.com/milocaetano/quantick/issues/338, read live before edits; retained exact issue JSON accompanies initial mission bytes.

> <!-- campaign-task:milocaetano/quantick#330/Q6 -->
> ## Context
>
> Parent: https://github.com/milocaetano/quantick/issues/330. Stable key Q6. At c3a92d58bb8a41ec4d78d73e60312b5f765b4da5, F1's IndicatorSlots/IndicatorHost reduces actual script-operation authority, but `indicator_manager.rs:850` still allocates and records native indicators directly on QuantickApp. The existing host already accepts IndicatorSource::Native. Meanwhile size ceilings do not prevent splitting broad root implementations across small files; blast_radius.rs explicitly remains advisory. Unchanged quantick-score v1.0 SE2 is4/5. This task combines an actual next-variant ownership refactor with accurately bounded structural drift protection; file splitting or field counts alone are not coupling proof.
>
> ## Scope
>
> Move existing native attachment into the same narrow operation owner, retaining the two-effect host and five bookkeeping collections. Add a registered guard protecting selected root/port shapes and separate aggregate source budgets for the explicit declarations/implementation blocks of QuantickApp and ChartState across files. Count shared root items, not all files under app/: focused owned units must remain able to grow. Preserve ordinary size/cycle/context guards, runtime/public/financial behavior and the score rubric. Keep the existing advisory diff report honest; do not promote its permissive parser to enforcement. No new native indicator, capability, root field, registry arm, dependency or general Rust resolver.
>
> ## Acceptance criteria
>
> - [ ] Native attachment allocates through the existing IndicatorHost and records its full TabSlot/SavedKind through IndicatorSlots. The root adapter resolves focus once, supplies the existing host/context, mirrors the returned layout intent and notes exactly one save. No third host effect or sixth context collection is added; no unrelated indicator variant changes.
> - [ ] Preserve exact native ID, empty values/default handling, human rather than operator ownership, full target bookkeeping, mirror/save behavior and unknown-ID visible error slots (never fallback to EMA). Existing toolbar/control reachability, script validation, permissions, saved-native restore and settings/library/hot-reload behavior remain unchanged.
> - [ ] A compiled independent fake-host fixture drives the actual native operation with known and unknown IDs and independently specified source/saved-kind/ownership/removal expectations. Real two-pane toolbar integration proves native attach/remove, mirroring and save intent. Existing native-catalog, unknown/saved-unknown native, F1 script and foreign-workspace registration/refusal tests remain meaningful and pass at final SHA.
> - [ ] The ordinary registered GUARDS/check/check_file/CLI/workspace-test path enforces normalized field names/types/visibility for QuantickApp, ChartState and IndicatorSlots, plus the exact IndicatorHost signatures. Equal-count replacement with a broad root reference, root-field addition or new host effect fails with target/path evidence. No source-hash freeze or cardinality-only claim substitutes for the contract.
> - [ ] Independently measured origin-SHA budgets count the union of production source-line spans for each protected root declaration and explicit inherent/trait impl, across all app source files. Separate no-growth caps prohibit paying one root's growth with the other's shrink. Moving/splitting the same broad implementation preserves its total and cannot create headroom. Owned IndicatorSlots/new focused-owner code is outside these root totals and must pass in either the same or a new file. No arbitrary registration exemption; this existing variant needs no new root registration.
> - [ ] Reuse the existing production classifier, ratchet arithmetic/registry and scratch infrastructure where sound, without changing existing meanings or ceilings. Document and test the exact formatted-source grammar: lexical comments/strings/raw strings/lifetimes and balanced delimiters, qualified/multiline inherent/trait impls, target fields/visibility and current lifetimes. Missing/duplicate targets, unreadable/corrupt input/baselines and unsupported protected syntax cannot silently become a clean empty result.
> - [ ] Explicit root-renaming import/type aliases and detectable protected scope inside item macros/external-path/include constructs receive accurate supported parsing or an unsupported-scope diagnostic. Preserve current legitimate callback aliases and unrelated declaration macros. No general macro-expansion/type-resolution guarantee is claimed. Document remaining synthesized-name, indirect-helper, root-carrying free-function and net-neutral rewrite limits; tests must not advertise those known gaps as rejected cases.
> - [ ] Negative scratch fixtures prove field/effect broadening, same-size authority-type replacement, root-code deposit below ordinary file ceiling, sibling-file split, trait-impl growth, cross-root budget transfer, qualified/multiline syntax and malformed/missing/unsupported input outcomes through registered and CLI interfaces. A realistic native-owned-code extension passes while equivalent broad-root deposition fails. A whole-directory budget that rejects this owned extension fails this task.
> - [ ] Record origin and final SHA, exact shape/budget contributions, semantic before/after responsibility changes, parser scope/known limits, realistic variant evidence and independent commands/results. Rates are rare attachment or repository tooling; any touched hot path requires its actual performance evidence. Full ordered fmt/clippy/build/workspace-test passes before every commit, guards after edits, and independent architecture/AI/source-first delivery plus exact-head CI close before merge.
> - [ ] Do not claim SE2=5 automatically or present the guard as complete semantic coupling analysis. Reassess the actual integrated SHA with the unchanged rubric and the realistic native/foreign-workspace evidence. If the declared bounded scanner cannot be implemented honestly without a general resolver or broad exemptions, record the blocker and revise the campaign plan; do not silently drop criteria, weaken guards or claim completion.
>
> ## Campaign record
>
> Owner class:autonomous. Priority:8. Risk:high (ownership and enforcement). Estimated scope: indicator_operations.rs and indicator_manager.rs; focused native/F1 tests; new guards extension-boundary/scanner/contracts/budgets, lib registry/tests and narrow documentation; unique mission/evidence. Dependencies:none; F1 prerequisite already integrated with green proof in parent. Start from latest integrated campaign SHA and recheck measured origin against intervening work without silently raising caps. Excludes Q2 schema builders, Q3 lane transport, Q4 Windows authority and Q5 workspace installation ownership.
>
> Owner/branch/worktree/PR/head/Project item:null until claim/readback. D4 maximum three independent disjoint implementation worktrees; serialized campaign merges and revalidation after base advance. Authority: https://github.com/milocaetano/quantick/issues/330#issuecomment-5568580109. Main merge exclusively human. Evidence:this issue, committed mission/contract/dossier, raw guard/behavior checks and linked PR/CI. Initial operation/repair counters0; preserve cumulative per-signature history. No paid services, deployment, new financial rules or rubric changes.
>
