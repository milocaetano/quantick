# Mission: prove retry-and-dedup or non-retryable-with-readback for every agent-reachable mutable control-plane capability

Generate a retry matrix from the capability registry — for every mutable
capability its declared idempotency policy, the behaviour the gateway enforces,
and the named readback an agent uses after a dropped or uncertain response —
guard it against drift, and prove each class through the real local gateway:
dropped-response retry with the same key for every `Optional` family, the
unknown-outcome refusal for a keyed action held past its deadline, and
readback reconciliation of an interrupted call for every non-retryable family.
Why it matters: rubric criterion AP4 sits at 4/5 because the unknown-outcome
branch and the `Forbidden`-family readback are proven only by reading and unit
tests; this earns the last point while keeping A+ gate 5 PASS, and makes the
next capability that forgets to say how it is reconciled fail a test.

Campaign child of https://github.com/milocaetano/quantick/issues/367 (task key
Q2); task issue https://github.com/milocaetano/quantick/issues/378. Base:
`origin/campaign/lean-a-plus` at `ff6ec56a`; PR base exactly
`campaign/lean-a-plus`.

**Tier:** `high` — the work sits on the control plane's authority boundary and
on the truthfulness of its public retry contract (the campaign parent classes
the risk as high), so the full interrogation budget (answered by the
coordinator's decisions), the full shape pass, a `medium` bug pass and
`delivery-review` in full are owed.

## Request ledger

- **R1** — A matrix generated from the capability registry covers 100% of the
  registered mutable (non-read-only) capabilities with policy, enforcement and
  readback columns; the readback names a read capability and the field that
  proves applied / not applied. Persisted as
  `docs/control-plane/retry-matrix.md`, regenerated by a documented command.
  *"Build the retry matrix from the generated capability registry"*; D2.
- **R2** — A drift test fails when a new mutable capability is registered
  without a row, when a row names a readback capability the inventory lacks,
  and when a descriptor's policy or named readback scope moves away from the
  row. *"a drift test fails on an unmapped new capability"*; *"a drift fixture
  asserting every non-read-only descriptor's policy and named readback
  scope"*.
- **R3** — Through the real local gateway, for at least one capability per
  family that declares `Optional` (layout, feed recovery, trade shaping), a
  dropped response retried with the same key returns the first recorded result
  and the effect happens once. A2; D3(a).
- **R4** — A keyed action held past its deadline so its outcome is unknown: the
  retry is refused as non-retryable / unknown-outcome, and the state is
  reconciled through the named read scope. The assessor's observable
  condition; D3(b).
- **R5** — For every non-retryable (`Forbidden`) capability an agent can reach,
  a test interrupts a call and resolves applied versus not applied through its
  named readback. A3; D3(c).
- **R6** — An independent read-only reassessment of AP4 at the PR head records
  the earned score with `file:line` evidence. A4.
- **R7** — The `RetainedBundle::new` constructor redesign carried from PR #391:
  fields private, digest / byte count / chunking computed by the constructor,
  the `store.rs` test fixture stops re-implementing the chunking — only if it
  stays internal and behaviour-preserving; say what changed. D1; issue comment.
- **R8** — No public contract change: no new capability, no changed descriptor
  policy, permission ceiling, profile or token, no wire field. Scopes, tokens
  and profiles are not widened. D1; issue scope.
- **R9** — Reconnect survival (#362) stays out of scope; the per-connection
  limit is stated plainly in the matrix document. D4.
- **R10** — Merged into `campaign/lean-a-plus` through a PR whose base is
  exactly that branch, with the merge read back. A5 (the merge is the
  coordinator's; this mission delivers the PR ready for it).
- **R11** — The purpose that judges the rest: AP4 earns its last point and A+
  gate 5 stays PASS. *"earning rubric criterion AP4's last point ... while
  keeping A+ gate 5 PASS"*.

## Decisions (from the coordinator; step 3 is answered by the prompt)

- **D1** — No public contract change. `RetainedBundle::new` only if internal and
  behaviour-preserving; otherwise a `human_decision`.
- **D2** — The matrix is generated from the capability registry, persisted as
  `docs/control-plane/retry-matrix.md` with a documented regeneration command
  and a drift test mirroring `inventory.rs` and PR #402's operability guard.
- **D3** — Transport-level evidence through the real local gateway: (a) one
  dropped-response retry per `Optional` family, (b) the unknown-outcome branch
  end to end, (c) an interrupted call resolved through its readback for every
  `Forbidden` family.
- **D4** — Reconnect survival stays out of scope; state the limit.
- **D5** — Per-request path only; the matrix is built at test/generation time.
  State per touched path in the PR body; no per-frame change.
- **D6** — Tier `high`: `arch-review` with `code-review` at `medium`, full shape
  pass, `ai-review` completion, `delivery-review` in full; at most three step-0
  rounds, remaining minor findings as named PR follow-ups.
- **D7** — `gh pr ready` once, cd-prefixed from the Bash tool, after reviews,
  markers and green CI; a denial is reported, never routed around. Never
  merge, never touch main.
- **D8** — No new file over 1,500 production lines and no touched file crossing
  it; guards green after each batch; context-ratchet growth paid by trimming or
  reported.

## Assumptions

- **S1** — *Trade shaping is unreachable in production.* `trade.*` sits under
  the `trader` ceiling no grant hands out, so no production connection can
  send it a key. D3(a) names the family anyway, so its dedup is proven through
  the real gateway under a **test-only** `trader` ceiling (a `#[cfg(test)]`
  enabling path that hands the ceiling to one test connection); production's
  ceiling, profiles and the panel are unchanged, and a second test proves a
  production grant still cannot reach any `trade.*` capability. Safe to assume
  because it widens nothing that ships and D1 forbids only shipped changes.
  *The source-first completeness pass flagged this as a reading of D1 that the
  trader may want to ratify; it is carried to the handoff as a
  `human_decision` (keep the test-only ceiling seam, or drop the trade-shaping
  dedup test and let the family rest on unreachability alone). Either answer
  changes no shipped line.*
- **S2** — *The feed-recovery test must not open a venue socket.*
  `feed.reconnect` on a live test tab spawns a real Binance transport, so the
  test aims it at a tab whose feed has left the feed table (`respawned:
  false`, a real answer) and counts how many times the application thread
  *began* the call: the test serves the queue through a `#[cfg(test)]` seam
  that runs the production `execute_on_ui` and reports each request's
  `UiRequest::started` flag. Safe because the dedup store is capability
  agnostic and "began once" is exactly the guarantee the key buys.
- **S3** — *"Interrupted" means the client lost its connection before any
  answer.* The applied arm drops the connection after the gateway queued the
  call (the application still runs it); the not-applied arm has the trader
  revoke the connection while the call is queued (the application refuses it
  before dispatch, and the socket closes). The client's view is identical in
  both, and only the readback
  over a fresh connection tells them apart — which is also the realistic
  variant #362 leaves every family in. Safe because both arms are real gateway
  paths, not test hooks.
- **S4** — *Holding an action past its deadline needs a test seam.* The
  gateway has no hook that lets an action run and stay unanswered; a
  `#[cfg(test)]` method serves one queued request through the real
  `execute_on_ui` and withholds its answer from the response worker. Safe
  because it is compiled out of the shipped binary and exercises the real
  `started` flag, settle window and store.
- **S5** — *Reachability is derived, not hand-kept.* A capability is reachable
  when the ceiling of a profile the gateway can hand out
  (`observer`, `annotator`, `cockpit`) holds all its required permissions; a
  gateway unit test pins that `configured_profile` never returns anything
  else, whatever is ticked.

## Acceptance criteria

- [x] **A1** — `docs/control-plane/retry-matrix.md` is generated by
      `cargo run -p quantick-app -- --dump-retry-matrix` and has one row for
      each of the registry's mutable capabilities with policy, enforced
      behaviour, reach, readback (capability, scope or event, field) and the
      proving test; its row count equals the inventory's non-read-only count.
      *Evidence:* the committed file; the byte-for-byte test in
      `crates/app/src/control/retry_matrix.rs`. → PR body. *(R1)*
- [x] **A2** — The drift guard fails on a mutable capability with no row, a row
      for no capability, a duplicate row, a row whose declared policy differs
      from the descriptor, a readback capability or scope the registry lacks
      or that is not read-only, a snapshot field path the scope's published
      schema does not carry, a readback that needs more than the capability's
      own grant plus the default reads, and a proving test that does not
      exist; each of those but the last is demonstrated by a mutation fixture
      (the last *is* a test over the real table; `NoCeiling` has no fixture
      because every registered capability has a ceiling); the fast guard in
      `crates/guards` checks the document against the inventory, with
      fixtures for a missing row, an orphan row, an unregistered readback, a
      lost marker and a missing file.
      *Evidence:* `cargo test -p quantick-app retry_matrix`,
      `cargo test -p quantick-guards`. → PR body. *(R1, R2)*
- [x] **A3** — Real-gateway tests prove, for layout, feed recovery and trade
      shaping, that a dropped keyed answer retried with the same key returns
      the first recorded outcome and the application began the call once.
      *Evidence:* the three named tests in
      `crates/app/src/app/tests/retry_readback_tests.rs`, green. → PR body.
      *(R3)*
- [x] **A4** — A real-gateway test holds a keyed action past its deadline and
      the settle window, observes the retry refused as non-retryable
      `control.timeout` with the read-back next step, observes that the retry
      did not act, and reconciles through the row's named read scope.
      *Evidence:* the named test, green. → PR body. *(R4)*
- [x] **A5** — For every reachable `Forbidden` capability (annotate create ×3,
      `annotate.remove`, `attention.mark.create`, `indicator.script.attach`,
      `indicator.script.detach`, `notify.popup`, `notify.toast`,
      `notify.sound`), a real-gateway test interrupts the call in an applied
      arm and a not-applied arm and resolves each through the row's readback;
      `trade.*` is proven unreachable by any production grant, keyed or not.
      *Evidence:* the named tests, green. → PR body. *(R5)*
- [ ] **A6** — An independent read-only AP4 reassessment at the reviewed head
      is posted with the earned score and `file:line` evidence. *Evidence:*
      the comment URL. → a comment on issue #378, linked from the PR body:
      in this repository `gh pr create` waits for both review markers, so no
      PR exists to comment on when the reassessment runs. *(R6, R11)*
- [x] **A7** — `RetainedBundle`'s fields are private, `RetainedBundle::new`
      computes digest, byte count and chunking, `into_manifest` reads them
      back, the `store.rs` fixture calls the constructor, and every evidence
      test passes unchanged. *Evidence:* the diff; `cargo test -p quantick-app
      evidence`. → PR body. *(R7)*
- [x] **A8** — No public contract change: the capability inventory and the
      published schemas are byte-identical, no descriptor policy, ceiling,
      profile or token changes. *Evidence:* `the_committed_inventory_is_what_the_generator_emits`
      and `published_schema_compatibility` green with no regeneration; the
      diff. → PR body. *(R8)*
- [x] **A9** — The matrix document states the per-connection limit (#362) and
      that a reconnect falls back to readback. *Evidence:* the quoted
      paragraph. → PR body. *(R9)*
- [ ] **A10** — The PR targets exactly `campaign/lean-a-plus` and is handed to
      the coordinator ready for its authorized merge. *Evidence:* `gh pr view`
      base; the handoff block. → handoff. *(R10)*

## Gates

- [x] **G1** — Every artifact English; conventional commits. `CLAUDE.md`;
      `arch-review` dimension 8; `crates/guards/src/language.rs`.
- [ ] **G2** — `cargo fmt --all -- --check`, `cargo clippy --workspace
      --all-targets`, `cargo build --workspace`, `cargo test --workspace` green
      at the final head, run one at a time; full CI green. `CLAUDE.md`.
- [x] **G3** — Performance declared per touched path by rate in the PR body;
      nothing per-frame, per-trade or per-depth grows. D5; issue G4.
- [ ] **G4** — `arch-review` over the exact diff against the campaign base with
      `code-review` at `medium`; every Blocker and Should-fix resolved or
      deferred in the PR body. `ai-review` completion recorded. D6.
- [x] **G5** — No new file over 1,500 production lines, no touched file crosses
      it; `cargo test -p quantick-guards` green; context ratchet green. D8.
- [x] **G6** — Adds something an operator does? No new capability; the new
      dump command is the named call for the matrix. `arch-review`'s *The
      second operator* graded on the generator and test seams.

- [x] **G7** — Writes only inside this worktree, and only to the files this
      child owns (`crates/app/src/control/*`, `crates/control/*`, the
      control-plane test modules under `crates/app/src/app/tests/`, new files,
      registration lines); siblings' files (`indicator_worker*`,
      `orderflow_worker*`, `state*`, `worker_progress*`, `app/health*`,
      `layers_tests.rs`, `crates/orderflow/src/projection*`/`config*`)
      untouched. The dispatch's *Worktree* section. *Evidence:* `git diff
      --name-only` against the base. → PR body.

## Source-first completeness pass

Run before implementation by an independent read-only reviewer (source
first, then this map): verdict COMPLETE. It added G7 (the ownership
constraint was stated only in the quoted request), noted that the handoff
block's field list lives in the quoted request, and flagged S1 for
ratification (now a handoff `human_decision`) and S3 as an interpretive
choice (kept, and stated in the tests' documentation).

## Implementation record

- The `trade.*` shaping dedup test runs under the test-only `trader` ceiling
  (S1); the feed test retires the tab's feed id after the frames that
  re-sync it and before the seam-served calls (S2).
- Found while proving A5: `analysis.indicators` needs the sensitive
  `observe.user_text`, so a caller granted the script tier and the default
  reads could not use it to reconcile an attach. The reach check was
  tightened from "the ceiling" to "the capability's own grant plus the
  default reads", which catches exactly that, and the script rows read the
  journal (`indicator.script.attached` / `.detached`) instead.
- Found by the full suite: `operability::sources` reads each file only up to
  its first `#[cfg(test)]` module, so the seam module is declared at the
  bottom of `gateway.rs`, beside `mod tests`.
- Local verification at the implementation head: `cargo fmt --all --
  --check`, `cargo clippy --workspace --all-targets`, `cargo build
  --workspace`, `env -u QUANTICK_BUBBLES cargo test --workspace` (2,023 app
  tests), `cargo test -p quantick-control`, `cargo test -p quantick-guards`,
  `sh .claude/hooks/guardrails_test.sh` (227 passed), each run on its own;
  the retry tests 8/8 green while the full app suite ran alongside.

## Review record

Round 1, over `52e35a1b..ccdd8f67`: `arch-review` step 0 (`code-review` at
`medium`, effort-first, no reuse notice) returned five findings, all
confirmed; the independent AP4 reassessment at `e1c3b5ff` (code-identical)
scored AP4 5/5 (narrow) and gate 5 PASS, and named overlapping weaknesses.
Repair batch 1 (one batch, one attempt per finding):

- **F1 / W1** — "Proven by" named a family test for rows it never called.
  Closed: `every_reachable_optional_row_replays_a_dropped_answer_and_begins_once`
  runs a keyed call and its retry for every reachable `optional` row and
  reads each row's field back; the rows name it.
- **F2** — an unnoted `attention.mark.create` matches any other unnoted mark,
  the trader's shortcut included. Closed: the row (and the `notify.*` rows)
  say the reconciling value must be unique to the call.
- **F3** — `drift` checked only snapshot rows. Closed: `Drift::ReadShape`
  refuses a journal row with no event kind, a selector on the wrong read, and
  any read without a readback grammar, with a fixture; the module docs say
  journal fields are proven by the transport tests, not a schema.
- **F4 / W7** — `GRANTABLE_PROFILE_IDS` sat under two orphaned doc comments.
  Closed: each item has its own doc again.
- **F5 / W7** — a run of spaces in an assertion message. Closed.
- **W5** — no transport test refused a key on a forbidden *action*. Closed:
  `every_reachable_forbidden_row_refuses_a_key_before_the_application`.
- The schema walk's hop bound is now the named `MAX_SCHEMA_HOPS`.

Found by the new table-driven test, **not fixed here** because the fix is a
wire-schema change D1 forbids: `LayoutResult.fraction` is an `f64` and the
codec refuses floating-point JSON, so `layout.focus.set`, `layout.pane.collapse`,
`layout.pane.expand`, `layout.pane.move` and `layout.preset.apply` (and any
successful `layout.pane.resize`) act, record their success, and then answer
the client a non-retryable `control.capability_unavailable` ("response could
not be encoded under the negotiated protocol rules"); `ResizeInput.fraction`
is an `f64` too, so a client can only send 0 or 1. The matrix's readback is
what reconciles those calls today. Carried to the handoff as a
`human_decision`: change `fraction` to a canonical decimal on both sides
(a published-schema change) in a follow-up.

Round 2, over `52e35a1b..daedc49f`: step 0 (`code-review` at `medium`,
effort-first, no reuse notice) returned three findings and two minor notes,
all confirmed. Repair batch 2 (second batch; each finding's first attempt):

- **R2-1** — the `feed.*` readback cannot tell whether *this* call ran: a
  reload normally hits a connected feed and reads `connected` either way.
  Closed as a documentation fix — nothing in the scopes changes per respawn,
  and adding a counter is a wire change D1 forbids. The rows now say they
  show the goal state; since the call ends in the same state however often it
  runs, a feed still stalled is the cue to send it again. The counter is a
  handoff `human_decision`.
- **R2-2** — the optional-row test only checked that a field existed.
  Closed: it now runs the rows in an order where each call really acts, and
  asserts each row's readback *moves* when the call applied (read until a
  rebuild lands) and *stays* when the handler refused it
  (`layout.pane.resize`). That surfaced two readback gaps, both closed:
  `tabs[].focused_pane` is a side, and two context charts share one, so
  `layout.focus.set` now reads the per-pane `tabs[].panes[].focused`, and
  `layout.pane.move` reads the `pane_id` order instead of `pane_index`.
  The third gap cannot be closed without a wire change: **no read projects
  the context column's collapse** (`tabs[].panes[].visible` counts a
  collapsed chart as shown). `layout.pane.collapse` / `layout.pane.expand`
  now declare *none: send it again*, a row kind `drift` allows only for an
  `optional` capability (`Drift::ResendNotSafe`, with a fixture), rendered as
  a readback still owed, and the test proves the effect lands and that
  sending it again changes nothing further. Projecting the collapse state is
  a handoff `human_decision`.
- **R2-3** — the annotate create rows identified the caller's drawing by an
  unauthenticated name. Narrowed: the rows also name the call's `tool_id`
  and say to keep one create per tool in flight; a request-scoped id in the
  event would be a wire change.
- Minor: the `branches` doc sat on `MAX_SCHEMA_HOPS` (fixed); the footer now
  counts rows and readbacks separately (29 rows, 27 readbacks).

## Closing steps

- **C1** — `delivery-review` returns PASS and records its marker.
- **C2** — The draft PR is open against `campaign/lean-a-plus`, CI green, and
  `gh pr ready` attempted once.

## Not applicable

- *Touches a hot path* — no per-frame, per-trade or per-depth code changes;
  the test seams are `#[cfg(test)]`, the store is untouched, and the evidence
  constructor runs once per capture on a response worker.
- *Touches anything user-visible* — no surface, hotkey or panel changes.
- *Adds a capability* — none; D1 forbids it.
- *Engine / determinism territory* — nothing under `crates/engine`.

## The request as received

The coordinator's dispatch, quoted verbatim as an attributed quotation (the
campaign coordinator to this child session):

> You are executing campaign child mission **Q2** of campaign #367 (https://github.com/milocaetano/quantick/issues/367) for milocaetano/quantick: prove retry-and-dedup or non-retryable-with-readback for every agent-reachable mutable control-plane capability, earning rubric criterion AP4's last point (currently 4/5) while keeping A+ gate 5 PASS. You are a subagent: you cannot ask the trader; decisions D1..Dn below answer the mission's step-3 questions. A doubt that would need a new decision becomes a `human_decision` in your handoff, never a guess, and never a contract change.
>
> ## Read first, in this order
> 1. `C:\src\quantick\CLAUDE.md` (authority boundary; operable without a hand; size rule: no file over 1,500 production lines; English everywhere).
> 2. `C:\src\quantick\.claude\skills\mission\SKILL.md` — you are a **campaign child at tier `high`**; follow steps 1, 2, 4, 5, 7, 8, 9. Step 3 is answered below. Step 6 is done (worktree, `mission-base`, `mission-tier`, guards armed); still run `cargo check -p quantick-app --all-targets` before the first edit.
> 3. `C:\src\quantick\docs\campaign\integration.md` (base `origin/campaign/lean-a-plus`, PR base exactly `campaign/lean-a-plus`, review key from `sh .claude/hooks/campaign_context.sh key "$WT"`), `C:\src\quantick\docs\workflow\delivery.md`.
> 4. The task: issue #378 (`gh issue view 378 --comments`): scope, A1..A5, gates, and the comment carrying `RetainedBundle::new` into this mission (the trader accepted that deferral on https://github.com/milocaetano/quantick/pull/391#discussion_r3995299951; read the thread for what the redesign is).
> 5. The baseline score's AP4 row and gate 5 evidence, in the report posted on the campaign parent: https://github.com/milocaetano/quantick/issues/367#issuecomment-5641522061 (AP4 = 4 at main `e8eb23e5`; next point: "one transport-level test holding a keyed action past its deadline, observing the non-retryable unknown-outcome refusal on retry and reconciling via a read scope; plus a drift fixture asserting every non-read-only descriptor's policy and named readback scope"; gate 5 PASS scoped by per-connection dedup (#362) and the `started` unknown-outcome branch covered only by unit tests).
> 6. `C:\src\quantick\docs\quality\quantick-score-rubric.md` (AP4 target, gate 5 wording, anchors), `docs/control-plane/control-contract.md` (§5.2 idempotency), `docs/control-plane/capability-inventory.md` (generated inventory; guard `crates/app/src/control/inventory.rs`), `crates/control/src/registry.rs` (`check_idempotency_key`), `crates/app/src/control/gateway/idempotency.rs`, `crates/app/src/control/contract.rs` and its new `contract/` siblings (S5 split them), `crates/app/src/control/{layout,recovery,trade,annotate,script,notify,actions,analysis}.rs`, the end-to-end idempotency tests in the control-plane test modules under `crates/app/src/app/tests/`, and PR #360's body for the design.
>
> ## Worktree (the only place you write)
> - `C:\src\quantick-worktrees\feat-retry-readback-matrix` (Git Bash `/c/src/quantick-worktrees/feat-retry-readback-matrix`), branch `feat/retry-readback-matrix`, cut from `origin/campaign/lean-a-plus` at `ff6ec56a`. Every command starts with `cd /c/src/quantick-worktrees/feat-retry-readback-matrix &&`. Never write to `C:\src\quantick` or other worktrees.
> - Siblings in flight own `crates/app/src/indicator_worker*`, `orderflow_worker*`, `state*`, `worker_progress*`, `app/health*` (Q3), `crates/app/src/app/tests/layers_tests.rs` (Q8) and `crates/orderflow/src/projection*`/`config*` (S8, merging now). You own `crates/app/src/control/*`, `crates/control/*`, the control-plane test modules under `crates/app/src/app/tests/` (the ones for control/gateway), new files you create (a retry matrix module and its generated document under `docs/control-plane/`), and registration lines.
>
> ## Decisions
> - D1: no public contract change: no new capability, no changed descriptor policy, no changed permission ceiling, profile or token, no wire field. `RetainedBundle::new` (accepted deferral) is an internal constructor redesign: do it only if it stays internal and behaviour-preserving, and say what changed; if it would change any public type or wire shape, stop and record a `human_decision`.
> - D2: the retry matrix is generated from the capability registry, not hand-kept: for every mutable (non-read-only) capability, its declared `IdempotencyPolicy`, the enforced behaviour (dedup store; refusal; not reachable by any grant), and the named readback an agent uses after a dropped or uncertain response (a read capability and the field that proves applied/not applied). Persist it as `docs/control-plane/retry-matrix.md`, regenerated by a documented command, with a drift test that fails when a new mutable capability is registered without a row or when a row names a readback capability that the inventory lacks (mirror how `inventory.rs` and PR #402's operability guard do it).
> - D3: transport-level evidence through the real local gateway (not the fake host): (a) for each `Optional` family (layout, feed recovery, trade shaping), at least one test that drops the response and retries with the same key, asserting the first recorded result and a single effect; (b) the unknown-outcome branch: a keyed action held past its deadline so the first call's outcome is unknown, the retry refused as non-retryable/unknown-outcome, and reconciliation via the named read scope; (c) for every `Forbidden`/non-retryable family, a test that interrupts a call and resolves applied vs not applied through its named readback.
> - D4: reconnect survival (#362) needs a control-contract change and a threat-model extension; it stays out of scope. State the per-connection limit plainly in the matrix document.
> - D5: performance: per-request path only; the matrix is built at test/generation time. State per touched path in the PR body; no per-frame change.
> - D6: tier `high`: `arch-review` with `code-review` at `medium`, full shape pass (authority boundary, operability, English); `ai-review` completion; `delivery-review` in full. At most three step-0 rounds; remaining minor findings ship as PR follow-ups named in the PR body.
> - D7: `gh pr ready` works with the cd-prefixed form from the Bash tool. Run it once after reviews, markers and green CI; if denied because the campaign tip moved or the PR is DIRTY, stop and report; the coordinator rebases. **Never use the PowerShell tool for `gh pr` commands; never merge; never touch main.**
> - D8: size rule: no new file over 1,500 production lines, no touched file crossing it; `cargo test -p quantick-guards` green after each batch. The context ratchet (`crates/guards/context-baseline.txt`) may count new Markdown; check `crates/guards/src/context.rs` and pay for growth by trimming, or report.
>
> ## Environment notes
> - Use `python`, not `python3`. If `cargo fmt` is blocked, run `rustfmt` directly then `cargo fmt --all -- --check`. Run the four checks one at a time, never `||` or `| head`; read whole failures. Do not wait on background commands; foreground with an adequate timeout.
> - App tests: `env -u QUANTICK_BUBBLES cargo test -p quantick-app ...`; control tests: `cargo test -p quantick-control`.
> - Check `df -h /c` before the first build; if under 15 GB free, `cargo clean` only in your own worktree and report.
> - If writing review markers into the git dir is denied by the permission classifier, put the exact `printf` lines in your handoff, marked pending.
> - Commit trailers: `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01VdP84bYEGeyCg7KwsNb1X2`.
> - Known CI load flake being fixed by Q8: `layers_tests::the_trade_paint_layer_switch_stops_the_marks`; report, rerun the job once at most.
>
> ## Delivery
> 1. Implement with the verification loop (`cargo check -p quantick-app`, the control/gateway targeted tests, `cargo test -p quantick-control`, `cargo test -p quantick-guards`).
> 2. Before every commit: `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets`, `cargo build --workspace`, `env -u QUANTICK_BUBBLES cargo test --workspace`, each separately.
> 3. Archive `GOAL.md` per mission step 8 (slug `retry-readback-matrix`) as the last commit before reviews.
> 4. Push; open a **draft** PR: `cd /c/src/quantick-worktrees/feat-retry-readback-matrix && gh pr create --draft --base campaign/lean-a-plus --title "feat(control): prove retry or readback for every reachable mutable capability" --body-file -` with a heredoc body per the PR template: tier, "Campaign child of #367 (Q2); closes on integration: #378", the matrix summary per family, the transport-level test list, the `RetainedBundle::new` outcome, local verification, then `🤖 Generated with [Claude Code](https://claude.com/claude-code)` and `https://claude.ai/code/session_01VdP84bYEGeyCg7KwsNb1X2`.
> 5. `/arch-review`, `/ai-review`, `/delivery-review`; resolve Blockers/Should-fixes; record markers with the shared key per integration.md.
> 6. Watch CI at the head (`gh pr checks <n> --watch`, bounded).
> 7. `gh pr ready <n>` once (D7).
> 8. Return a HANDOFF BLOCK: issue; branch; worktree; PR URL; head SHA; base tip; matrix counts per policy class; the tests added and what each proves; `RetainedBundle::new` outcome; an honest self-assessment of AP4 against the rubric anchors and of gate 5; review verdicts with URLs; markers yes/no; CI run URL and conclusion; findings closed/open; repair batches; ready accepted or denied; any human_decision; the coordinator's next action.
