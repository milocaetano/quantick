{
  "$defs": {
    "CanonicalDecimal": {
      "pattern": "^-?(0|[1-9][0-9]*)(\\.[0-9]*[1-9])?$",
      "type": "string"
    },
    "PaperClosedTradeSnapshot": {
      "properties": {
        "closed_unix_ms": {
          "format": "int64",
          "type": "integer",
          "x-unit": "unix_milliseconds"
        },
        "entry_aggregate_id": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireU64"
            },
            {
              "type": "null"
            }
          ],
          "description": "Aggregate ids of the prints that opened and closed the position — the\naudit trail back to the tape. Absent only on rows loaded from a\nversion-1 history file, which did not record them."
        },
        "entry_price": {
          "$ref": "#/$defs/CanonicalDecimal"
        },
        "exit_aggregate_id": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireU64"
            },
            {
              "type": "null"
            }
          ]
        },
        "exit_price": {
          "$ref": "#/$defs/CanonicalDecimal"
        },
        "exit_reason": {
          "type": "string"
        },
        "maximum_adverse_excursion_points": {
          "anyOf": [
            {
              "$ref": "#/$defs/CanonicalDecimal"
            },
            {
              "type": "null"
            }
          ],
          "description": "Worst excursion against the average entry while the position was open,\nin points and never negative. Absent for version-1 rows: unknown is\nnot zero.",
          "x-unit": "points"
        },
        "opened_unix_ms": {
          "format": "int64",
          "type": "integer",
          "x-unit": "unix_milliseconds"
        },
        "pnl_points": {
          "$ref": "#/$defs/CanonicalDecimal",
          "x-unit": "points"
        },
        "quantity": {
          "$ref": "#/$defs/CanonicalDecimal"
        },
        "side": {
          "type": "string"
        }
      },
      "required": [
        "side",
        "quantity",
        "entry_price",
        "exit_price",
        "opened_unix_ms",
        "closed_unix_ms",
        "pnl_points",
        "exit_reason"
      ],
      "type": "object"
    },
    "PaperOrderSnapshot": {
      "properties": {
        "cancel_at": {
          "anyOf": [
            {
              "$ref": "#/$defs/CanonicalDecimal"
            },
            {
              "type": "null"
            }
          ]
        },
        "flat_only": {
          "type": "boolean"
        },
        "kind": {
          "type": "string"
        },
        "order_id": {
          "$ref": "#/$defs/WireU64"
        },
        "placed_unix_ms": {
          "description": "Venue time of the last print seen when the order was placed. The\nsimulator has no clock of its own.",
          "format": "int64",
          "type": "integer",
          "x-unit": "unix_milliseconds"
        },
        "price": {
          "anyOf": [
            {
              "$ref": "#/$defs/CanonicalDecimal"
            },
            {
              "type": "null"
            }
          ],
          "description": "Limit price for a limit, trigger price for a stop, absent for a market\norder — which has no price of its own by definition."
        },
        "quantity": {
          "$ref": "#/$defs/CanonicalDecimal"
        },
        "side": {
          "type": "string"
        },
        "stop_loss": {
          "anyOf": [
            {
              "$ref": "#/$defs/CanonicalDecimal"
            },
            {
              "type": "null"
            }
          ]
        },
        "take_profit": {
          "anyOf": [
            {
              "$ref": "#/$defs/CanonicalDecimal"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "order_id",
        "side",
        "kind",
        "quantity",
        "flat_only",
        "placed_unix_ms"
      ],
      "type": "object"
    },
    "PaperPositionSnapshot": {
      "properties": {
        "average_entry_price": {
          "$ref": "#/$defs/CanonicalDecimal"
        },
        "open_points": {
          "anyOf": [
            {
              "$ref": "#/$defs/CanonicalDecimal"
            },
            {
              "type": "null"
            }
          ],
          "description": "Open profit in points — price units times quantity, signed. Points and\nnot currency: the workspace knows no per-instrument tick value, and a\nnumber that cannot be computed honestly is not published. Absent\nbefore any mark exists.",
          "x-unit": "points"
        },
        "quantity": {
          "$ref": "#/$defs/CanonicalDecimal"
        },
        "side": {
          "type": "string"
        }
      },
      "required": [
        "side",
        "quantity",
        "average_entry_price"
      ],
      "type": "object"
    },
    "TabPaperSnapshot": {
      "properties": {
        "armed_strategy": {
          "description": "The named exit ladder the ticket is armed with, if any.\n\nWhat the *next* order will carry, which the order rows below cannot\nsay: they are what already exists. An operator asked \"why is nothing\nprojecting\" has to be able to read this, and until it was here the\nanswer lived only in pixels.",
          "type": [
            "string",
            "null"
          ]
        },
        "armed_strategy_refusal": {
          "description": "Why the armed ladder cannot be used, when it cannot. Absent when it\nis usable, and absent when nothing is armed — a strategy that draws\nnothing and says nothing is the bug this field exists to prevent.",
          "type": [
            "string",
            "null"
          ]
        },
        "closed_trade_count": {
          "$ref": "#/$defs/WireU64"
        },
        "closed_trades": {
          "items": {
            "$ref": "#/$defs/PaperClosedTradeSnapshot"
          },
          "maxItems": 64,
          "type": "array"
        },
        "closed_trades_page_start": {
          "$ref": "#/$defs/WireU64",
          "description": "Row of the whole ledger that `closed_trades[0]` stands for. Zero unless\nthe page was cut; published because the page is the ledger's *tail*, so\nwithout it a row number here cannot be placed in the ledger the count\nabove describes."
        },
        "closed_trades_truncated": {
          "description": "True when `closed_trades` was cut at its page limit. The newest rows\nare kept: a ledger is read from its end.",
          "type": "boolean"
        },
        "flat": {
          "type": "boolean"
        },
        "position": {
          "anyOf": [
            {
              "$ref": "#/$defs/PaperPositionSnapshot"
            },
            {
              "type": "null"
            }
          ]
        },
        "provenance": {
          "description": "Where these rows come from, named rather than implied.",
          "type": "string"
        },
        "risk_amount": {
          "anyOf": [
            {
              "$ref": "#/$defs/CanonicalDecimal"
            },
            {
              "type": "null"
            }
          ],
          "description": "What that size stands to lose at the entry's own stop. Absent for the\nsame reasons as `risk_quantity`."
        },
        "risk_blocks_entry": {
          "description": "Whether the trader's lock is refusing this entry right now.",
          "type": "boolean"
        },
        "risk_currency": {
          "description": "The currency `risk_amount` is in. Nothing here converts between\ncurrencies, so a reader comparing two tabs must compare these too.",
          "type": [
            "string",
            "null"
          ]
        },
        "risk_quantity": {
          "anyOf": [
            {
              "$ref": "#/$defs/CanonicalDecimal"
            },
            {
              "type": "null"
            }
          ],
          "description": "The size the risk per trade derived, absent when it derived none —\nthe mode is off, or nothing here could be sized."
        },
        "risk_sentence": {
          "description": "The sentence the ticket is showing, verbatim.\n\nProduced by the same function that renders it on screen rather than a\nsecond wording kept in step by hand: an operator reading this and a\ntrader reading the ticket are looking at one sentence. Absent only\nwhen the mode is off and there is nothing to say.",
          "type": [
            "string",
            "null"
          ]
        },
        "risk_state": {
          "description": "What the risk per trade makes of the entry the ticket is holding:\n`off`, `sized`, `short_of_budget`, `capped_at_max`,\n`clamped_at_minimum`, `instrument_unknown`, `no_budget` or `refused`.\n\nThe state, not the pixels. An operator asked \"why is the size 1\" has\nto be able to read the answer rather than infer it from a number.",
          "type": "string"
        },
        "ruler_ticks": {
          "description": "How far the aim's ruler stands from an entry, in ticks; zero when it\nis not in use. Sticky across aims, so a reader needs it to explain\nwhat the next order would carry.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "selected_trade_row": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireU64"
            },
            {
              "type": "null"
            }
          ],
          "description": "Row of the selected closed trade in the whole ledger — the same\nnumbering as `closed_trade_count`, not a position in the page above.\nSubtract `closed_trades_page_start` to index `closed_trades`; a row\nbelow that start was cut and is not on this page."
        },
        "symbol": {
          "type": "string"
        },
        "tab_id": {
          "$ref": "#/$defs/WireU64"
        },
        "tick_size": {
          "$ref": "#/$defs/CanonicalDecimal",
          "description": "One tick of this tab's instrument, so `ruler_ticks` can be read as a\nprice rather than a count."
        },
        "working_order_count": {
          "$ref": "#/$defs/WireU64"
        },
        "working_orders": {
          "items": {
            "$ref": "#/$defs/PaperOrderSnapshot"
          },
          "maxItems": 128,
          "type": "array"
        },
        "working_orders_truncated": {
          "description": "True when `working_orders` was cut at its page limit; the count above\nstill reports every order the simulator holds.",
          "type": "boolean"
        }
      },
      "required": [
        "tab_id",
        "symbol",
        "provenance",
        "flat",
        "ruler_ticks",
        "tick_size",
        "risk_state",
        "risk_blocks_entry",
        "working_orders",
        "working_order_count",
        "working_orders_truncated",
        "closed_trades",
        "closed_trade_count",
        "closed_trades_truncated",
        "closed_trades_page_start"
      ],
      "type": "object"
    },
    "WireU64": {
      "pattern": "^(0|[1-9][0-9]*)$",
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "tabs": {
      "items": {
        "$ref": "#/$defs/TabPaperSnapshot"
      },
      "type": "array"
    }
  },
  "required": [
    "tabs"
  ],
  "title": "PaperSnapshot",
  "type": "object"
}
