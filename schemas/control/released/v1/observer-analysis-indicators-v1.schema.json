{
  "$defs": {
    "CanonicalDecimal": {
      "pattern": "^-?(0|[1-9][0-9]*)(\\.[0-9]*[1-9])?$",
      "type": "string"
    },
    "IndicatorFailureSnapshot": {
      "description": "Why an indicator is not answering. The stable `detail` is a code a client\nbranches on; the underlying message is withheld whenever the trader's own\nscript wrote it.",
      "properties": {
        "bar_index": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireU64"
            },
            {
              "type": "null"
            }
          ],
          "description": "The bar the evaluation failed on, when the failure names one."
        },
        "detail": {
          "type": "string"
        },
        "state": {
          "description": "`error` — disabled until rebuilt — or `stale`: a hot reload failed and\nthe previously running version is still evaluating.",
          "type": "string"
        },
        "user_text_redacted": {
          "description": "True when a message exists but is the trader's own text.",
          "type": "boolean"
        }
      },
      "required": [
        "state",
        "detail",
        "user_text_redacted"
      ],
      "type": "object"
    },
    "IndicatorInputSnapshot": {
      "properties": {
        "default": {
          "type": [
            "string",
            "null"
          ]
        },
        "kind": {
          "description": "`int`, `float`, `bool`, `color`, `string` or `source`.",
          "type": "string"
        },
        "name": {
          "description": "Stable identifier — the persistence key and the script argument name.",
          "type": "string"
        },
        "options": {
          "description": "The fixed choices, when the input declares any.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "text_present": {
          "description": "True when this is a `string` input the script left unconstrained, and a\nvalue is bound. Free text is the trader's own words, so the value is\nwithheld and only its presence reported; a `string` input constrained\nto a fixed option set is an enumeration, not prose, and its value is\npublished above.",
          "type": "boolean"
        },
        "title": {
          "type": "string"
        },
        "value": {
          "description": "The bound value, rendered in the input's own vocabulary. Absent for a\nfree-text input: see `text_present`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "name",
        "title",
        "kind",
        "text_present",
        "options"
      ],
      "type": "object"
    },
    "IndicatorSnapshot": {
      "properties": {
        "committed_bar_count": {
          "$ref": "#/$defs/WireU64",
          "description": "Committed bars this indicator has evaluated. Tracked rather than\nderived from the plot columns: an indicator whose whole output is\ncandle paint declares no plots at all."
        },
        "failure": {
          "anyOf": [
            {
              "$ref": "#/$defs/IndicatorFailureSnapshot"
            },
            {
              "type": "null"
            }
          ],
          "description": "Why this indicator is not answering, when it is not. Absent means it\nevaluated cleanly."
        },
        "hidden": {
          "description": "The eye toggle. A hidden indicator keeps evaluating, so its readings\nbelow are still current.",
          "type": "boolean"
        },
        "inputs": {
          "description": "Every declared input paired with the value currently bound to it —\nwhat the settings dialog would open with.",
          "items": {
            "$ref": "#/$defs/IndicatorInputSnapshot"
          },
          "type": "array"
        },
        "kind": {
          "description": "The constructor it was added through (`native.cvd`, `script.zigzag`),\ndurable across remove and re-add in a way the slot is not.",
          "type": "string"
        },
        "ordinal": {
          "description": "Which instance of that kind this is, assigned once at birth. Two EMAs\non one pane are told apart by this, never by position.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "overlay": {
          "description": "True = drawn over the price chart; false = its own sub-pane below it.",
          "type": "boolean"
        },
        "plots": {
          "items": {
            "$ref": "#/$defs/PlotSnapshot"
          },
          "type": "array"
        },
        "preview_present": {
          "description": "Whether a forming-bar preview frame exists right now.",
          "type": "boolean"
        },
        "short_title": {
          "type": [
            "string",
            "null"
          ]
        },
        "slot_id": {
          "$ref": "#/$defs/WireU64",
          "description": "The slot the running instance answers to. Reused after a remove, so it\naddresses an instance *now* and is not an identity across a session."
        },
        "source_kind": {
          "description": "`native` or `script` — whether we wrote it or the trader did. It\ndecides whether any diagnostic text is theirs, and so redacted.",
          "type": "string"
        },
        "title": {
          "type": "string"
        }
      },
      "required": [
        "slot_id",
        "kind",
        "source_kind",
        "ordinal",
        "title",
        "overlay",
        "hidden",
        "plots",
        "inputs",
        "committed_bar_count",
        "preview_present"
      ],
      "type": "object"
    },
    "PaneIndicatorsSnapshot": {
      "properties": {
        "indicator_count": {
          "$ref": "#/$defs/WireU64"
        },
        "indicators": {
          "items": {
            "$ref": "#/$defs/IndicatorSnapshot"
          },
          "maxItems": 64,
          "type": "array"
        },
        "indicators_truncated": {
          "type": "boolean"
        },
        "pane_id": {
          "$ref": "#/$defs/WireU64"
        },
        "side": {
          "$ref": "#/$defs/PaneSideDto"
        }
      },
      "required": [
        "pane_id",
        "side",
        "indicators",
        "indicator_count",
        "indicators_truncated"
      ],
      "type": "object"
    },
    "PaneSideDto": {
      "enum": [
        "flow",
        "time"
      ],
      "type": "string"
    },
    "PlotSnapshot": {
      "properties": {
        "base_color": {
          "type": "string"
        },
        "latest_value": {
          "anyOf": [
            {
              "$ref": "#/$defs/CanonicalDecimal"
            },
            {
              "type": "null"
            }
          ],
          "description": "The most recent committed value of this column. Absent when the column\nhas no rows yet, or when the value is not finite — a gap in a series is\na gap, never a zero."
        },
        "offset_bars": {
          "description": "Horizontal shift in bars; positive is rightward.",
          "format": "int32",
          "type": "integer",
          "x-unit": "bars"
        },
        "plot_index": {
          "$ref": "#/$defs/WireU64",
          "description": "Index of the column this spec describes, in declaration order."
        },
        "renders_as_marker": {
          "description": "True when the column renders as markers instead of through its style.",
          "type": "boolean"
        },
        "style": {
          "type": "string"
        },
        "title": {
          "type": "string"
        }
      },
      "required": [
        "plot_index",
        "title",
        "style",
        "offset_bars",
        "renders_as_marker",
        "base_color"
      ],
      "type": "object"
    },
    "TabIndicatorsSnapshot": {
      "properties": {
        "panes": {
          "items": {
            "$ref": "#/$defs/PaneIndicatorsSnapshot"
          },
          "type": "array"
        },
        "tab_id": {
          "$ref": "#/$defs/WireU64"
        }
      },
      "required": [
        "tab_id",
        "panes"
      ],
      "type": "object"
    },
    "WireU64": {
      "pattern": "^(0|[1-9][0-9]*)$",
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "tabs": {
      "items": {
        "$ref": "#/$defs/TabIndicatorsSnapshot"
      },
      "type": "array"
    }
  },
  "required": [
    "tabs"
  ],
  "title": "IndicatorsSnapshot",
  "type": "object"
}
