[package]
name = "quantick-trading"
description = "Venue-neutral order vocabulary and the TradingVenue port every execution backend implements"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
quantick-engine = { path = "../engine" }
rust_decimal = "1"
