//! quantick-app — desktop chart (egui/wgpu planned).
//!
//! A consumer of `quantick-engine`, never the other way around.

fn main() {
    println!(
        "quantick {} — skeleton; chart not implemented yet",
        env!("CARGO_PKG_VERSION")
    );
}
