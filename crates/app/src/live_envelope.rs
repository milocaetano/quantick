//! The live workload quantick supports, as numbers the code reads: the
//! figures, and the caps they imply, live in `quantick-backpressure`'s
//! `envelope`, beside the queue discipline that holds them.

pub(crate) use quantick_backpressure::envelope::*;
