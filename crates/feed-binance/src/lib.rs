//! quantick-feed-binance — live aggTrades feed from Binance public endpoints.
//!
//! Produces the trade stream that `quantick-engine` consumes. Requires no
//! API key: Binance market data endpoints are public.
