[package]
name = "quantick-pine"
description = "Quantick Pine: the Pine v5 subset dialect frontend for scripted indicators"
version.workspace = true
edition.workspace = true
license.workspace = true

# Hand-rolled lexer/parser/interpreter — zero dependencies beyond the
# indicator runtime (supply-chain minimalism; full control over spans and
# errors; the language is small).
[dependencies]
quantick-indicators = { path = "../indicators" }
