# Hosted owner preparation: external repair01

Authority: issue480#5722205787, owner-hosted-repair01-decision.md. This is adoption material only. Product source, runner/parser/suffix, budgets, selectors and invocation order are unchanged. Product builds, samples, live transport, remote actions and native application execution have not run. Root binds and releases each finite push separately; unresolved JSON/YAML placeholders refuse admission.

## Compile phase

hosted.py uses the byte-identical six-file supervisor/import closure in supervisor_closure, including the frozen measure.py import. supervisor-closure.json binds each byte. The private worker waits for stdin until Windows Job ownership attaches, then starts Cargo. Each command has its own receipt directory and fixed 3600-second timeout within the unchanged 300-minute job cap. Cargo exit0, finished state, no timeout/interruption and proven zero active Job processes are required. Contained postbuild helpers may be terminated: their orphan flag and observations remain recorded. This phase-only rule does not change edit-loop measurement or collector admission.

The OS-essential child environment explicitly sets PYTHONDONTWRITEBYTECODE=1 so worker imports cannot mutate the exhaustive helper closure. Nested tool paths use validated paths and explicit parent creation. Tools bindings must include all six closure files, supervisor-closure.json and the other adopted tools, with exact hashes.

After BOTH profiles of a side close, source/archive/artifacts are verified. Only enumerated non-artifact files below that fresh exact B-target/C-target may be removed. Links/junctions/escaping paths are refused; retained exe/PDB files are hashed before and after disposal. Source, archive, logs, manifests and cache are not deleted. Disposal inventory records planned and removed files. Capacity snapshots record actual free disk, RAM and target/cache occupancy. The package contains the before-package capacity history; the separately uploaded capacity.jsonl also includes after-package occupancy.

ZIP packaging/restoration and artifact transfer use streaming copies. All package members retain exact hashes and regular-file metadata. This does not establish hosted disk/RAM capacity: actual baseline exe/PDB payload alone is approximately 497 MB per side; dependency/cache and ZIP occupancy remain runtime facts. Phase1 uses exact immutable Git ZIPs and original+LF+suffix02 source overlays. Raw Cargo default marker ['default'] is retained and admitted only after source default=[] proof. No --no-default-features change occurs.

## Collection phase

transport.py binds exact successful run ID/attempt1/carrier and artifact ID/name/run/digest. Failed outer and inner bytes remain, with observed hashes/sizes and sanitized phase/API identities; always-upload includes all transport files. Signed redirect URLs and credentials are never receipt content. HTTP redirect/auth behavior remains unexecuted on live GitHub; offline fixtures cover identity refusals, ambiguous/missing payload, outer/inner digest failures, API refusal and unresolved release.

launcher.py derives a separate release from a hash-bound immutable template with empty allowed_processes. Root must bind exact Python/pwsh executable paths/hashes and policy decision. Two independently queried exact current-Python/immediate-pwsh PID/creation/parent records must agree; only that one parent is inserted. Template is unchanged, and no other release field changes. Wrappers, recycled identities, unknown image hashes, tampering and unresolved policy refuse with sanitized evidence. This adds no process-name or ancestor exemptions. The collector remains byte-identical and still rejects compiler/product processes. Actual phase2 policy and release remain PENDING.

Collection restores to the same fresh C:/qperf paths and requires equal OS/image/Python/CPU/runtime DLL identities. Fresh hosted VMs may fail that comparison. No relaxation, rerun or fallback is granted. The immutable exact-before/branch/attempt1 gate records github.sha; root reviews and publishes the actual after SHA before pushing, avoiding a self-referential binding hash. Clean carrier diff must match the declared tooling-only map plus its binding file.

## Validation and limits

All original tests remain. Repair tests use synthetic files and mocked transport/CIM; the sole live-process fixture runs harmless Python children through the unchanged Windows Job supervisor, never Cargo or Quantick. Final log owner-hosted-repair01-all-tests03.log records 26 PASS. all-tests02 retains one test-module BOM/mojibake syntax failure, corrected by removing the six accidental encoding bytes. Earlier all-tests01 passed25; compile-tests01 passed21. Separate harmless-owned-proof contains two terminal process receipts and unchanged closure verification.

Frozen runner SHA256: 847b00607322fa0c0f260fd18c56b7376cb84b6bd8830b35532169a2f77c2312. Parser: 8b5b32768a1ec1fd9234fd0fe7fb283a52096f8222edfc18a5a095f35fda60ca. Suffix02: e34b57b032e8d88cb68b85606b7f479b97446db5f5cb93595a053dff9013340a.

Windows2025/Python3.13.7 availability, artifact transfer limits, installed toolchain/runtime availability, actual hosted Job behavior under Actions, final carrier bindings, launcher identities and exclusive collection window remain release prerequisites. Local harmless Job tests are not hosted compile or performance evidence. No repository setting/main change, F2 attempt reuse, native enable or performance acceptance is claimed. Originals remain in owner-hosted-repair01-originals; the earlier compile-frozen snapshot remains immutable.
