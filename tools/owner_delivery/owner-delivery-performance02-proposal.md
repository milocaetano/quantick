# Owner delivery: concrete performance preparation

Proposed protocol only. No sample, build, external source materialization or execution is released by this document. Finish the current G6/I2/R1 source and independent reconciliation first. Candidate identity must be the clean, reviewed combined commit; it is not a moving branch. Baseline remains campaign80da32426e6e2f7a2a962ab8054d986897461b6d. This is a separate owner-delivery comparison, not a retry of SER1, F2 or another exhausted finding.

Reuse the fixed FRAME preflight in frame-performance-preflight-independent-review.md. The concrete identical action suffix is owner-action-measurement-suffix02.rs. It appends only to disposable exports of app/tests/drawings_tests.rs, preserving all product source, existing helpers and assertions. The source reviewer must compare helper semantics across the two exact revisions and bind both export manifests and suffix hash before compilation. No measurement edits enter a task branch or frozen rubric/measure/score files.

Three actual actions use the existing real-egui secondary drag, settling frame, primary press and primary release: Profile, Retracement, Projection. Each observation creates a new app/context through app_with_history(200), retaining its existing per-app scratch-home isolation, Tick1/1400x900/live-strip-off fixture and actual registered Human placement. Prepare input before the measured interval. Release-frame timing retains FullOutput until afterward; output checks, JSON construction and printing occur after timing. Allocation observations use the existing test-only per-thread tally in a separate invocation. The existing test allocator is still compiled into the timing binary; this observer cost is identical on both sides and is not represented as zero.

Each invocation has exactly 10 checked warmups and 100 retained observations per action. Emit every warmup and measured observation, action-specific metric and semantic output: actual tool, all anchor bar/price bits and timestamps, feed_id plus symbol market identity and Flow identity. Reject missing/disabled control, a preexisting drawing, wrong resulting drawing count/tool/anchor count, wrong fractional anchors, persistent temporary range or refusal. Require baseline/candidate semantic output equality without normalizing geometry, prices or timestamps. Fresh drawing IDs are omitted because they do not describe drawing semantics.

Timing order: B1,C1 / C2,B2 / B3,C3, six complete invocations with no best-of repeats, trimming, adaptive sample count or pooled actions. Allocation mode uses the same six-invocation order independently; compare the actual allocations/bytes/reallocations/copy counts, with no new allowance. The successful profile and Fibonacci paths do not replace existing invalid-input/refusal correctness tests. No claim is made about GPU latency or isolated core-dispatch nanoseconds.

Dense workload: unchanged control_idle_dense_replay_benchmark, default optimized test profile, exact existing8000/30/600/64 fixture. Same three alternating pairs, all summaries retained. Preserve the existing greater-than10% median CPU-average investigation trigger; values below it are not automatically accepted. Action latency has no preexisting numeric allowance: report per-action paired mean/median/p99/worst and all samples; any consistent adverse shift or uncertain tail remains unresolved for independent analysis. Do not choose a tolerance from observed results.

Existing long-session work/time budgets remain literal and must be checked with the unchanged release long fixture on both exact exports, one complete invocation per side. Do not repurpose its within-session1.50 scaling limit as a candidate regression allowance. The ordinary fast-count coverage can be reused only from the same source with its input-bound proof. Final reviewers must identify any affected hot path not exercised by these existing fixtures rather than treating an empty benchmark result as coverage.

Build all required binaries on both sides before collection, with equal Rust toolchain, Cargo settings/profile/features, unchanged lock dependencies and isolated targets. Preserve executable/PDB hashes, source/overlay manifests and commands. Sanitize inherited Quantick scenario overrides equally; do not print credentials or trader paths. At collection, verify no compiler, competing benchmark or native automation is active. Preserve process evidence at each pair. Stop on overlap, fixture/assertion failure, missing record or failed budget; retain the first failure and all already collected evidence, with no retry-until-green.

Before samples, root must publish the exact candidate, compiled identities, source review and finite collection release. These existing checks may run without touching the trader's native window or enabling a control instance. Native correctness and H4 shared-directory failure remain separately unresolved; this comparison does not waive either. G3 performance-before-PR and all current-head review/CI obligations remain unchanged.

Preflight refinements: the identical suffix02 includes actual feed_id as well as symbol; suffix01 remains retained. The existing allocator realloc_copy_bytes and largest_realloc_copy are upper bounds on bytes that a realloc may copy, not measurements of bytes physically copied. Baseline/current helper equality requires semantic inspection, including AppLaunch defaults and per-app scratch isolation; byte equality of wrappers is not presumed. Final candidate/source manifests and execution identities remain pending.
