# Initial independent behavioral evaluation — cases 7–11

| Case | Next action and verification | Review, escalation and merge |
|---|---|---|
| **7: changed base** | Revalidate affected behavior and run the full ordered fmt/clippy/build/test loop; identical child text does not establish identical inputs. | Applicable **full independent reviews** on the new base/current key. Old-base reports cannot support delta-only approval. No human input ordinarily needed; merge waits for current reviews, final-head CI and existing destination authority. |
| **7: missing raw outputs** | Restore actual outputs if recoverable; otherwise rerun affected verification under the full-loop path. A remembered PASS is insufficient. | Missing runtime output does not automatically erase an available independent review report: follow-up is permissible if base/scope stay unchanged and evidence impact is clear; otherwise full review. No merge while required evidence is unproven. |
| **8: two unsuccessful batches** | Keep **F7**, its identity and counters. Checkpoint and escalate now; renaming F9 cannot obtain another attempt. | Two consecutive batches closing none of their targeted findings triggers the stop before the nominal three-batch limit. No PASS/merge with F7 open; independent campaign work may continue. |
| **8: three successful repairs, another omission open** | Record each closure as progress and each independent discovery separately, but checkpoint/escalate because the mission’s three-batch budget is exhausted. | No fourth repair batch or deferral without an explicit user decision; no PASS/merge with the omission open. |
| **8: final batch closes everything** | Finish delivery; do not escalate merely because batch 3 was used. | Current required reviews and green final-head CI permit delivery within existing destination authority. |
| **9** | Reconcile and merge the ready authorized child first, using the serialized head-pinned gate. Then prioritize the bounded repair and its build-host needs. Pending repair occupies an implementation slot; start at most one additional independent implementation only if it does not compete for that host. | A campaign tip advance can invalidate the other child’s reviews/verification; refresh accordingly. Give the final main PR to the user at `ready_for_evaluation`; green CI does not authorize an agent main merge. **No per-child goal prompt.** Reassess any final PR whose head changed after the child integration. |
| **10: retry/merge skill edit** | Classify as operational instructions: guards, hygiene, links, applicable skill validators, shared hook suite and independent behavioral exercises; full final-head CI. Use runtime verification only if executable/contract/test inputs or uncertain impact require it. | Review the actual changed authority/behavior and all affected conclusions at the current key; retain tier gates and mandatory bug pass. Full review if scope changes, impact is uncertain, or prior reports are absent. “Evidence-only” is invalid. New merge permission is not created by writing the skill. |
| **10: approved deferral** | Retain the original outcome/ID as withdrawn or deferred with approval source; record under `## Deferred` and repeat in PR body. Inspect the entire delta/artifacts and run applicable record/guards/links checks. Runtime reuse requires the complete proof contract; a scope edit alone does not force runtime reruns. | Deferral changes scope, so obtain **applicable full reviews**, not traceability-only follow-up. Existing approval needs no renewed human permission. PASS must disclose the deferral; merge authority remains separate. |
| **11** | First adopt merged main through the campaign’s normal reviewed synchronization PR, with existing authorization, validation and merge gate. Record source main revision and resulting campaign checkpoint. Coordinate the running child’s ownership; rebase/synchronize it normally and refresh invalidated validation/reviews. | Do not apply new rules directly to the old writer’s branch or bypass its current gate. Preserve requests, failures, finding IDs and counters. After recorded adoption, reassess the escalation: a raw-count false stall may resume within remaining authority/budget; exhausted budgets and real user decisions remain blocked. Workflow adoption is neither permission nor a counter reset. |

**Rule locations:** `docs/workflow/delivery.md` sections **Review once, follow up on the delta**, **Finding identity and bounded progress**, **Validation follows changed inputs**, **Finish work before starting more**, and **Adopt changes in an existing campaign**; `docs/campaign/integration.md` **Authority and lifecycle**, steps 4–7, and **Branch-bound task context**; `docs/campaign/workflow.md` **Run loop** and **Validation by risk**; `docs/campaign/state.md` **Ownership, recovery and attempts**; `.claude/skills/delivery-review/SKILL.md` steps 4–6; `.claude/skills/mission/SKILL.md` steps 2, 4, 8–9; `.claude/skills/ship/SKILL.md` steps 1 and 4–6.

**Contradictory instruction encountered:** `ship` step 3 orders draft publication before step 4’s reviews, but `mission` step 8 orders architecture/delivery reviews before PR creation, and `delivery-review` step 6 says `pr-gate` denies PR creation without both current markers. The workflow needs an explicit draft-publication exception or consistent ordering; none should be inferred to bypass the gate.

# Narrow follow-up — draft/readiness instructions

The explicit ordering contradiction is resolved in the current files. Ship step 3 (lines 45–50) publishes the draft before step 4 reviews, while mission step 8 (lines 274–276) now expressly permits draft publication before reviews and waits for current reviews and CI before ready. CLAUDE.md lines 75 and 79, arch-review lines 92–107, and delivery-review lines 327–330 consistently gate readiness rather than draft creation. Delivery-review lines 143–144 now allow an existing draft when assembling the dossier. CONTRIBUTING.md and the PR template do not impose a conflicting review/draft order; they require applicable verification and final-head CI.

Residual stale rationale remains in delivery-review lines 220–223: it says no C closing step can have happened while reviewing and that two are unblocked by the verdict. Mission still defines “PR is open” as a closing step, and a draft PR may now already exist. This does not alter the correct next action or authorize bypassing a gate, but that rationale should be corrected. Arch-review's description still calls the review “pre-PR”; this is cosmetic given its explicit readiness rule.

Cases 7–11 decisions otherwise remain unchanged. This follow-up inspected instructions only; it did not verify hook implementation, run tests/builds, edit repository files, post, merge or stamp. The only write was this explicitly authorized external evidence artifact.
