# Workflow synchronization evidence

Q10 / [issue #350](https://github.com/milocaetano/quantick/issues/350) adopts
user-merged main `9b8495538a9a1af16fce61dcd4805d112fc0c71e` (PR #345) into
campaign `99f7e57fe3d475fb78cc336848278bdc2b4f2f0f`. The isolated branch is
`fix/campaign-workflow-sync`. Both histories remain parents of the synchronization;
the campaign receives it only through its reviewed child PR.

## Combined obligations

The new delivery contract retains source reconciliation, stable finding IDs,
finite repair accounting, input-bound verification and current independent
review/CI gates. The campaign's separate `ai-review-complete` attestation and
unresolved-thread checks remain required at every tier. Changed head/base/key
invalidates approval; the completion producer still verifies its full identity
before review, after review and after durable publication.

Conflict resolutions preserve both owners:

| Overlap | Resolution |
| --- | --- |
| AI review | Keep the campaign's complete recording procedure and report identity; use main's delivery-contract reference for bounded progress. |
| Mission and ship | Keep main's draft/review/validation lifecycle and explicitly retain AI completion at every tier. |
| Guard registry | Register both instruction-link checking and the native extension boundary, with their original implementations and policies. |
| Guard integration tests | Keep both independently named tests and literal registry coverage; extend the literal array to eleven entries. |
| Extension CLI fixture | Supply the newly required instruction entrypoints/directories in its synthetic repository; preserve every original boundary assertion. Missing documentation must not masquerade as the expected boundary refusal. |

Automatic overlap merges in the hook suite, hook documentation and AGENTS.md
retain both inputs. AGENTS.md keeps canonical generated capability discovery
and main's proportional verification reference; minor equivalent wording fits
its existing ceiling. The context budget, thresholds and existing ceilings are
unchanged from the user's main revision.

The new context scope counts `docs/campaign/`. Two pre-existing historical
records are now byte-identical archives under the already designated
`docs/workflow/evidence/` directory: Q1 implementation/validation and the
coordinator's original validation/simulation. Their original paths remain
linked entrypoints. No operative rule is moved out of accounting; canonical
skills and campaign contracts retain the obligations. This classification is
not a claim of reduced architectural coupling or a scoring increment.

## Validation and provenance

Executable guard/hook and fixture inputs change, so this synchronization uses
the full code verification path. No earlier runtime results substitute for
its local ordered fmt/clippy/build/workspace-test loop or final-head CI.
The full local test command uses child-process `RUST_TEST_THREADS=1`, with no
filter, ignored-test change or relaxed threshold. Normal CI remains unchanged.

External immutable receipts are under
`C:/Users/camil/AppData/Local/Temp/quantick-architecture-a/Q10-validation/`:

- `preedit-receipt.json`, `request-only.md`, original/v2 preflight reports and
  `preflight-map-repair.json`: guard arming and source-checked mission before
  the first merge/edit; two initial map omissions closed by one correction.
- `merge-main.json`, `conflict-resolution-repaired.json`,
  `historical-record-relocation.json` and `source-proof.json`: exact sources,
  overlap decisions, archive digests and complete staged-input associations.
- Timestamped command JSON/log pairs: actual exit status, source head/status,
  explicit environment overrides and output digest for guards, shared hooks,
  progress-tool tests and ordered workspace verification.
- `behavioral-exercises.md/json`: independent decisions for the supplied
  workflow cases and source-bound historical-record classification.
- `failure-register.json`: actual integration failures and their disposition;
  failed source specimens remain preserved. No failure is relabeled passing.

The PR publishes the actual results and final source identity after execution.
This document specifies the evidence and its limits; it does not assert that
future reviews, CI or integration have already happened. Runtime/application,
financial, public schema, dependency and rubric equality are checked against
the campaign source; all retained historical exceptions remain historical.

## Adoption and closing

The synchronization is delivered through the existing campaign integration
gate. Only after that reviewed merge does the coordinator adopt compact
operation journals, partitioned checkpoints above 24 KiB and durable review
progress. Existing legacy counters remain preserved rather than being guessed
into the new schema. New missions record their actual initial history.

Post-merge work records the actual main source and campaign result, reassesses
the exact candidate under unchanged quantick-score v1.0, updates issue/Projects,
and returns control to the campaign coordinator. Q2-Q5 recovery already merged;
it is not repeated. Main's historical baseline is not its new current score.
Final campaign-to-main review/evaluation and user merge remain separate.
