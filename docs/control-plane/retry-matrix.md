# Control plane retry matrix

<!-- generated by `quantick-app --dump-retry-matrix`; do not edit -->

What a client does when a call that changes something did not answer:
retry it under the same idempotency key, or read the state back. One row
per mutable capability the running application registers; read-only
capabilities change nothing and need no reconciling.

This file is generated; a hand edit is a guard failure, not a correction.
The rows live in `crates/app/src/control/retry_matrix.rs`, and the
generator refuses to render while any of them disagrees with the registry.
To change a row, change the table or the capability and regenerate:

```sh
cargo run -p quantick-app -- --dump-retry-matrix \
  > docs/control-plane/retry-matrix.md
```

`cargo test -p quantick-app retry_matrix` compares this file with the
generator and every row with the registry; `cargo test -p quantick-guards`
compares it with the [capability inventory](capability-inventory.md) in a
second.

## Reading a row

**Policy** is the `idempotency` the descriptor publishes. **Enforced** is
what the gateway does with it:

- *deduplicated per connection* — a call carrying an idempotency key is
  recorded against the connection that made it. The same key with the same
  input answers the first call's recorded outcome without acting again; the
  same key with different input is `control.idempotency_conflict`; a retry
  that arrives while the first call is still running is
  `control.request_in_progress` (retryable — ask again). A call the
  application began and had still not answered a full request window after
  its deadline records a non-retryable `control.timeout` whose next step is
  to read the state back: its outcome is unknown, and retrying the key cannot
  learn it.
- *key refused; not retryable, read back* — a call carrying a key is
  refused before dispatch (`control.invalid_request`). Sending the call
  twice acts twice, so a client whose call did not answer reads the state
  back instead.
- *refused before dispatch: no grant reaches it* — the capability's
  ceiling is one no grant hands out, so no connection can call it, keyed or
  not. The row records what its readback will be when that changes.

**Reach** is the lowest profile whose ceiling admits the capability.
Every readback is readable by a connection granted the capability's own
permissions and the default read grant (the generator refuses a row that
is not), so reconciling never needs a scope the trader did not already
tick for the call. A capability no grant reaches is held to its ceiling
instead, and its row says which further scope its readback needs.
**Readback** is the read that shows the effect — a `snapshot.read` scope,
or `events.read` filtered to one journal event kind — and **Field** the
path within it, `[]` stepping into an array. **Applied when** says what the
field shows if the call took effect. **Proven by** names the transport
tests, in `crates/app/src/app/tests/retry_readback_tests.rs`, that exercise
the row through the real local gateway.

To reconcile, take the reading *before* the call — the scope's value, or
an `events.read` cursor at `latest` — and compare it with the same read
afterwards. A snapshot read can be retried freely: reads are read-only.

## The guarantee ends with the connection

Deduplication is scoped to the connection that made the call. A client
that reconnects arrives as a new principal, and its keys are new keys: the
same key re-executes rather than replaying. That is deliberate — the only
identities that survive a reconnect are the bearer token, which every
client of one grant shares, and a client-supplied name nothing
authenticates, and scoping records by either would let one client replay
another's answer. Widening it needs a durable client identity the
handshake proves, which is a contract change tracked in
[#362](https://github.com/milocaetano/quantick/issues/362). Until then a
client whose connection dropped with a call in flight reconciles *every*
row — `optional` ones included — through its readback, never by retrying
the key on the new connection.

## Coverage

| Enforced | Capabilities |
| --- | --- |
| deduplicated per connection | 12 |
| key refused; not retryable, read back | 10 |
| refused before dispatch: no grant reaches it | 7 |
| **Total** | **29** |

## Capabilities

| Capability | Policy | Enforced | Reach | Readback | Field | Applied when | Proven by |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `annotate.arrow.create` | forbidden | key refused; not retryable, read back | `annotator` | `snapshot.read` `analysis.drawings` | `tabs[].panes[].drawings[].author.client_name` | a drawing authored by the caller that the pre-call reading lacked is there | `an_interrupted_annotation_is_resolved_by_its_readback`, `every_reachable_forbidden_row_refuses_a_key_before_the_application` |
| `annotate.label.create` | forbidden | key refused; not retryable, read back | `annotator` | `snapshot.read` `analysis.drawings` | `tabs[].panes[].drawings[].author.client_name` | a drawing authored by the caller that the pre-call reading lacked is there | `an_interrupted_annotation_is_resolved_by_its_readback`, `every_reachable_forbidden_row_refuses_a_key_before_the_application` |
| `annotate.remove` | forbidden | key refused; not retryable, read back | `annotator` | `snapshot.read` `analysis.drawings` | `tabs[].panes[].drawings[].drawing_id` | the named `annotation_id` is no longer listed | `an_interrupted_annotation_is_resolved_by_its_readback`, `every_reachable_forbidden_row_refuses_a_key_before_the_application` |
| `annotate.zone.create` | forbidden | key refused; not retryable, read back | `annotator` | `snapshot.read` `analysis.drawings` | `tabs[].panes[].drawings[].author.client_name` | a drawing authored by the caller that the pre-call reading lacked is there | `an_interrupted_annotation_is_resolved_by_its_readback`, `every_reachable_forbidden_row_refuses_a_key_before_the_application` |
| `attention.mark.create` | forbidden | key refused; not retryable, read back | `annotator` | `events.read` `attention.mark.created` | `payload.note` | an event after the pre-call cursor carries the call's own `note`; send a note unique to the call, since an unnoted mark (the trader's shortcut included) matches any other | `an_interrupted_attention_mark_is_resolved_by_its_readback`, `every_reachable_forbidden_row_refuses_a_key_before_the_application` |
| `feed.reconnect` | optional | deduplicated per connection | `cockpit` | `snapshot.read` `feed.status` | `tabs[].connection_state` | the tab's feed is connecting or connected again | `a_dropped_feed_recovery_answer_is_replayed_and_the_recovery_runs_once`, `every_reachable_optional_row_replays_a_dropped_answer_and_begins_once` |
| `feed.reload` | optional | deduplicated per connection | `cockpit` | `snapshot.read` `feed.status` | `tabs[].connection_state` | the tab's feed is connecting or connected again | `a_dropped_feed_recovery_answer_is_replayed_and_the_recovery_runs_once`, `every_reachable_optional_row_replays_a_dropped_answer_and_begins_once` |
| `indicator.script.attach` | forbidden | key refused; not retryable, read back | `annotator` | `events.read` `indicator.script.attached` | `payload.script.slot_id` | an event after the pre-call cursor names a new `slot_id` | `an_interrupted_script_attach_or_detach_is_resolved_by_its_readback`, `every_reachable_forbidden_row_refuses_a_key_before_the_application` |
| `indicator.script.detach` | forbidden | key refused; not retryable, read back | `annotator` | `events.read` `indicator.script.detached` | `payload.script.slot_id` | an event after the pre-call cursor names the `slot_id` asked for | `an_interrupted_script_attach_or_detach_is_resolved_by_its_readback`, `every_reachable_forbidden_row_refuses_a_key_before_the_application` |
| `layout.focus.set` | optional | deduplicated per connection | `cockpit` | `snapshot.read` `workspace.summary` | `tabs[].focused_pane` | the tab's focused pane is the one asked for | `every_reachable_optional_row_replays_a_dropped_answer_and_begins_once` |
| `layout.pane.collapse` | optional | deduplicated per connection | `cockpit` | `snapshot.read` `workspace.summary` | `tabs[].panes[].visible` | the context panes are not visible | `every_reachable_optional_row_replays_a_dropped_answer_and_begins_once` |
| `layout.pane.expand` | optional | deduplicated per connection | `cockpit` | `snapshot.read` `workspace.summary` | `tabs[].panes[].visible` | the context panes are visible | `every_reachable_optional_row_replays_a_dropped_answer_and_begins_once` |
| `layout.pane.move` | optional | deduplicated per connection | `cockpit` | `snapshot.read` `workspace.summary` | `tabs[].panes[].pane_index` | the moved pane's `pane_id` sits at the index asked for | `every_reachable_optional_row_replays_a_dropped_answer_and_begins_once` |
| `layout.pane.resize` | optional | deduplicated per connection | `cockpit` | `snapshot.read` `workspace.summary` | `tabs[].split_fraction` | the tab's split is the share asked for | `every_reachable_optional_row_replays_a_dropped_answer_and_begins_once` |
| `layout.pane.set_interval` | optional | deduplicated per connection | `cockpit` | `snapshot.read` `chart.summary` | `panes[].bar_spec` | the pane's bar spec is the interval asked for | `every_reachable_optional_row_replays_a_dropped_answer_and_begins_once` |
| `layout.preset.apply` | optional | deduplicated per connection | `cockpit` | `snapshot.read` `workspace.summary` | `tabs[].layout` | the tab's layout is the preset asked for | `every_reachable_optional_row_replays_a_dropped_answer_and_begins_once` |
| `layout.tab.create` | optional | deduplicated per connection | `cockpit` | `snapshot.read` `workspace.summary` | `layouts[].name` | one more layout than the pre-call reading listed | `a_dropped_layout_answer_is_replayed_and_the_layout_is_made_once`, `every_reachable_optional_row_replays_a_dropped_answer_and_begins_once`, `a_keyed_action_held_past_its_deadline_is_refused_as_unknown_and_reconciled_by_readback` |
| `layout.tab.rename` | optional | deduplicated per connection | `cockpit` | `snapshot.read` `workspace.summary` | `layouts[].name` | the layout carries the name asked for | `every_reachable_optional_row_replays_a_dropped_answer_and_begins_once` |
| `layout.tab.switch` | optional | deduplicated per connection | `cockpit` | `snapshot.read` `workspace.summary` | `layouts[].active` | the layout asked for is the active one | `every_reachable_optional_row_replays_a_dropped_answer_and_begins_once` |
| `notify.popup` | forbidden | key refused; not retryable, read back | `annotator` | `events.read` `notify.raised` | `payload.message` | an event after the pre-call cursor carries the call's own `message`; send one unique to the call | `an_interrupted_notification_is_resolved_by_its_readback`, `every_reachable_forbidden_row_refuses_a_key_before_the_application` |
| `notify.sound` | forbidden | key refused; not retryable, read back | `annotator` | `events.read` `notify.raised` | `payload.message` | an event after the pre-call cursor carries the call's own `message`; send one unique to the call | `an_interrupted_notification_is_resolved_by_its_readback`, `every_reachable_forbidden_row_refuses_a_key_before_the_application` |
| `notify.toast` | forbidden | key refused; not retryable, read back | `annotator` | `events.read` `notify.raised` | `payload.message` | an event after the pre-call cursor carries the call's own `message`; send one unique to the call | `an_interrupted_notification_is_resolved_by_its_readback`, `every_reachable_forbidden_row_refuses_a_key_before_the_application` |
| `trade.instrument.set_money` | optional | refused before dispatch: no grant reaches it | none (`trader` ceiling) | `events.read` `trade.ticket.changed` | `payload.asked` | an event after the pre-call cursor carries the caller's input as `asked` | `no_production_grant_reaches_a_trade_capability_keyed_or_not` |
| `trade.order.bracket` | forbidden | refused before dispatch: no grant reaches it | none (`trader` ceiling) | `snapshot.read` `session.paper` (also needs `observe.paper`) | `tabs[].working_orders[].stop_loss` | the named order carries the stop and target asked for | `no_production_grant_reaches_a_trade_capability_keyed_or_not` |
| `trade.order.cancel` | forbidden | refused before dispatch: no grant reaches it | none (`trader` ceiling) | `snapshot.read` `session.paper` (also needs `observe.paper`) | `tabs[].working_orders[].order_id` | the named order is no longer working | `no_production_grant_reaches_a_trade_capability_keyed_or_not` |
| `trade.order.place` | forbidden | refused before dispatch: no grant reaches it | none (`trader` ceiling) | `snapshot.read` `session.paper` (also needs `observe.paper`) | `tabs[].working_orders[].order_id` | an order the pre-call reading lacked is working, or a position opened | `no_production_grant_reaches_a_trade_capability_keyed_or_not` |
| `trade.risk.set` | optional | refused before dispatch: no grant reaches it | none (`trader` ceiling) | `snapshot.read` `session.paper` (also needs `observe.paper`) | `tabs[].risk_state` | the ticket's risk state follows the basis asked for | `no_production_grant_reaches_a_trade_capability_keyed_or_not` |
| `trade.ruler.set` | optional | refused before dispatch: no grant reaches it | none (`trader` ceiling) | `snapshot.read` `session.paper` (also needs `observe.paper`) | `tabs[].ruler_ticks` | the ruler stands at the distance the call answered with | `a_dropped_trade_shaping_answer_is_replayed_and_the_ticket_changes_once`, `no_production_grant_reaches_a_trade_capability_keyed_or_not` |
| `trade.strategy.select` | optional | refused before dispatch: no grant reaches it | none (`trader` ceiling) | `snapshot.read` `session.paper` (also needs `observe.paper`) | `tabs[].armed_strategy` | the ticket is armed with the strategy asked for | `no_production_grant_reaches_a_trade_capability_keyed_or_not` |

29 mutable capabilities registered, 29 with a readback.
