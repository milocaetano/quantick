Final local workspace verification — temporary right-drag volume profile

Base: origin/main d8de9a1b557d3965c923feb8998bfd226e9dd067
Implementation commit: 0fc730c2f17bc44a9ee4f9fd5276de9ec4d81e8e
Date: 2026-09-13 America/Sao_Paulo

PASS  cargo fmt --all -- --check
      Exit 0; no formatting changes required.
PASS  cargo clippy --workspace --all-targets --quiet
      Exit 0; no warning or error emitted.
PASS  cargo build --workspace --quiet
      Exit 0.
PASS  cargo test --workspace --quiet
      Exit 0; every workspace test binary and doctest passed. The largest
      application suite ran 2,052 tests: 2,045 passed and 7 were ignored.

The host defines `QUANTICK_BUBBLES` globally as a path outside this repository.
The verification process removed that variable so the suite consumed the
embedded, versioned bubble fixture. The exact base checkout was separately
confirmed to reproduce the two fixture-test failures with the host override
and to pass them without it. No product source or test was weakened for this
host-specific condition.

The only commit after this verification is the required mission archive and
its evidence ledger. GitHub CI is the exact-head verification for that final
documentation-only commit.
