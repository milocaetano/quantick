# Does the suite still run the same tests?

    before (origin/main, e0ae2ac) : 1894 passed; 0 failed; 4 ignored
    after  (this branch)          : 1894 passed; 0 failed; 4 ignored

Identical. No test was added, removed, renamed or skipped: the twelve files
in crates/app/src/app/tests/ are byte-identical to origin/main, and the only
change under that directory is two import lines and a comment in mod.rs
(see generated.txt and ledger-check.md).

cargo test --workspace on the final tree: 93 'test result: ok' lines, no
FAILED, no failures block, exit 0.

## One dropped test seen during the run-up, and why it is not this branch

An earlier run of cargo test -p quantick-app reported 1893 passed; 1 failed.
It started while cargo test --workspace was still releasing the machine, and
the name was lost to a pipe. Four consecutive runs immediately afterwards
were clean at 1894/0/4, as was every workspace run. This matches a known
intermittent here: the app crate drops one test at random under CPU
contention, at the same rate on main as on a branch. Recorded rather than
hidden, because a green claim built on one run is not evidence.
