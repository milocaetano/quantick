# UI validation

The refactor makes no visible layout or interaction change relative to pull request 306. It preserves the `REC` control and trades-bar selector while adapting them to current owners. The hook registry was regenerated and its byte-for-byte test passes; `DEAL_RECORDING`, `DEALS_DIR`, and `BARS_MENU` remain declared. Fresh screenshots and visual review are therefore not applicable.
