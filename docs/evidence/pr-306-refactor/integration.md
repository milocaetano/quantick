# Pull request 306 integration

The branch was merged with `origin/main` at `e8eb23e` in a clean worktree. Current application owners (`ChromeState`, split `Tab` modules, and `SpecSelector`) were retained, and the deal-bar feature was adapted to them. The original feature worktree was left untouched.
